# Start a Tutor Session

```bash
./bin/learn start
```

Codex then reads the selected module materials and follows the tutor skill. Use `./bin/learn continue` to resume and `./bin/learn status` to inspect local progress.
