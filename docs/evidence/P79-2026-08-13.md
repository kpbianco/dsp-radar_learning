# P79 retained implementation evidence — 2026-08-13

## Claim boundary

P79 implements the complete repository artifacts and deterministic contracts
for **Compare SAR Resolution, Aperture Length, and Windowing**. Evidence here
covers static repository validation, bounded standard-library Python scientific
oracles, learner-CLI behavior, and retained source/document review.

No `matlab`, `octave`, or `octave-cli` executable was available. No MATLAB
script execution, rendered-figure inspection, MATLAB version/toolbox result,
runtime performance, or MATLAB timeout/cancellation result is claimed.

The model is deterministic synthetic data. It is not physical hardware/HIL,
bench, field, RT1/RT2, real-time, operational radar, Unreal, signing, staging,
release, deployment, or production validation.

## Governance, current state, ownership, concurrency, and CI inspection

Before editing, branch `agent/p79` pointed to `5131107`, the merged P78 commit
and `origin/main`. P78 was implemented and satisfied P79's dependency. The
canonical batch's `10069b5` baseline is historical product provenance;
implementation continued from the newer integrated P78 state.

`git worktree list --porcelain` showed this target worktree plus a separate
detached repository worktree, which was untouched. `git ls-files -u` reported
no index conflicts; no Git lock, running MATLAB/Octave/verification process,
or competing P79 implementation was found. No repository `CODEOWNERS`,
P79-specific ADR, separate safety rule, compatibility record, or additional
repository current-state/handoff file exists beyond the inspected governance,
architecture, implementation/tutor protocols, canonical handoff, decisions,
risks, dependency modules, and tests.

The only pre-existing dirty paths were operator-managed activation changes in
forbidden `contracts/active-batch.yaml` and `contracts/repo-profile.yaml`.
Their P79 content was read and preserved, not authored or modified by this
implementation. CI is `.github/workflows/quality.yml`: Ubuntu, Python 3.12, a
ten-minute job, and `./scripts/agent-verify.sh`. The workflow and all other
forbidden paths were preserved.

The principal agent retained edit and integration ownership. One bounded
read-only physics audit checked the equations, monostatic sampling convention,
metric definitions, and resource design. An authoring risk audit found no
implementation defect and identified one low-severity walkthrough sentence
that invited changing a sweep whose reviewed runtime contract fixes the
approved values; the wording was corrected to require restoration before
completion checks. The later independent system-risk review and its test-only
repair are recorded under exact commands below.

## Acceptance map

| Acceptance item | Implemented evidence | Validation class |
| --- | --- | --- |
| Preserve the guiding question | The exact question appears in all five module artifacts; manifest identity other than P79 status is unchanged. | Static test and curriculum validation |
| Deterministic baseline, visible controls, labeled plots/metrics, sweep, broken case | `experiment.m` uses private seed `7901`, visible SI controls, five labeled figure groups, bandwidth/aperture/weighting/sampling comparisons, sparse-track failure, and retained metrics. | Static contract plus simulated Python oracle |
| Explain model and limiting cases without syntax-first teaching | `lesson.md` connects explicit frequency and exact slant-range phase sums to `c/(2B)`, `lambda*R/(2L)`, taper, the monostatic alias interval, eleven limiting cases, and model boundaries. | Static/manual content review |
| Guide baseline, one-variable changes, failure interpretation, connection | `walkthrough.md` sequences isolated cuts, the three-point image, two resolution sweeps, window tradeoff, undersampling failure, fresh recovery, cancellation, rollback, and P32–P80 connections. | Static/manual content review |
| Test interpretation and prediction | `checks.md` provides 30 answered checks, a completion checklist, and four-part teach-back rubric. | Static test/manual learning gate |
| Declare dependencies and expose essential behavior | README declares P12/P33/P30–P32/P61–P63/P75–P78 and base MATLAB R2016b; source exposes both coherent sums and Hamming weights without opaque SAR/toolbox calls. | Static regression test |
| Set implemented status only after required files/checks | All five artifacts existed and focused curriculum/scientific checks passed before the P79 manifest handoff was treated as validated. | Curriculum and focused tests |
| Separate static from MATLAB runtime evidence | This evidence and module prose explicitly state runtime unavailability and claim limits. | Evidence-content test |

## Deterministic simulated-oracle results

The permanent Python oracle independently evaluates the MATLAB equations using
only the standard library. Metrics use unclipped linear magnitude and locate
half-power crossings plus first local minima on the reviewed axes.

| Oracle metric | Result | Permanent criterion |
| --- | ---: | ---: |
| Private-seed first three uniforms | `0.06183614375, 0.28006411939, 0.03765066319` | repeatable without global RNG mutation |
| Range HPBW at `100/200/400 MHz` | `1.323672 / 0.661832 / 0.330912 m` | strictly decreases with bandwidth |
| Range one-sided first null | `1.495 / 0.745 / 0.375 m` | within `0.02 m` of `c/(2B)` |
| Range PSL at `100/200/400 MHz` | `-13.2611 / -13.2612 / -13.2612 dB` | uniform-response PSL between `-13.6` and `-12.8 dB` |
| Cross-range HPBW at `10/20/30 m` aperture | `1.296767 / 0.656269 / 0.439323 m` | strictly decreases with aperture |
| Cross-range one-sided first null | `1.465 / 0.740 / 0.495 m` | within `0.06 m` of `lambda*R/(2L)` |
| Cross-range PSL at `10/20/30 m` | `-13.2439 / -13.2561 / -13.2576 dB` | uniform-response PSL between `-13.6` and `-12.8 dB` |
| Uniform/Hamming cross-range HPBW | `0.439323 / 0.649660 m` | taper width greater than `1.3` times uniform |
| Uniform/Hamming cross-range PSL | `-13.2576 / -42.6121 dB` | taper PSL more than `20 dB` lower |
| Hamming white-noise SNR efficiency | `-1.370311 dB` | explicit sensitivity cost between `-2` and `-1 dB` |
| Largest response outside `+/-1.5 m`, spacing `0.25/1/5 m` | `0.091467 / 0.093258 / 0.999999` | broken sparse response exceeds `0.95`; dense below `0.15` |
| Predicted sparse alias interval | `3.000000 m` | `lambda*R/(2d)` with monostatic two-way phase |
| Three-target dense/sparse image peak | `0.8707206715 / 0.8883984455` | exact reviewed standard-library oracle values |
| Composite response at `x=-3/+3 m` versus true `x=0` response | dense `2.58% / 5.65%`; sparse `99.99% / 100.02%` | dense below `7%`; broken sparse above `99%` |
| Dense composite-image recovery | exact deterministic equality | fresh focus from unchanged seeded scene |
| Scheduled coherent contributions | `5,254,493` | exact executed/predicted agreement, below `6,000,000` |

The oracle also rejects nested, Boolean, nonfinite, unordered, nonpositive,
even-count, incompatible-target, off-grid, out-of-support, aliased-dense,
oversized-grid, generator, and operation-ceiling controls. Helper checks reject
invalid seeds/counts, nonfinite range requests, and aperture endpoints that do
not land on the declared sample grid. CLI fixtures cover explicit selection,
status rollback refusal, restoration, future metadata/status compatibility,
three-second process timeouts, and isolation from repository learner state.

## Figure and metric inventory

1. **Baseline point-spread function:** range offset (m) and cross-range offset
   (m) versus normalized magnitude (dB); HPBW (m), first-null scale (m), PSL
   (dB); three-target local image versus cross-range and ground-range offset
   (m) with truth markers.
2. **Bandwidth sweep:** `100/200/400 MHz` range cuts (dB), HPBW (m), and
   one-sided first-null resolution (m) versus transmitted bandwidth (MHz).
3. **Aperture sweep:** `10/20/30 m` cross-range cuts (dB), HPBW (m), and
   one-sided first-null resolution (m) versus synthetic-aperture length (m).
4. **Window trade:** uniform/Hamming aperture weight (linear) versus platform
   position (m); normalized cross-range magnitude (dB), HPBW/PSL, coherent
   peak change (dB), and white-noise SNR efficiency (dB).
5. **Sparse failure/recovery:** cross-range cut (dB) for `0.25/1/5 m` spacing;
   seven retained broken looks over the `30 m` track; broken/recovered local
   images (dB) versus range/cross-range (m); predicted alias interval (m), far
   peak (linear), and exact recovery flag.

Immutable ceilings are 513 frequency samples, 201 aperture samples, 4,001 cut
samples, 20,000 image pixels, four targets, four cases per sweep, 32 private
generator values, 6,000,000 coherent contributions, 2,000,000 eight-byte
live-value equivalents, `0.9*pi` dense adjacent phase step, and five tagged
figure groups.

## Exact commands and results

Runtime discovery:

```text
command -v matlab
command -v octave
command -v octave-cli
```

All returned no executable and no output. The future actual-MATLAB test has a
five-minute subprocess timeout and checks deterministic rerun, caller RNG
isolation, resource accounting, core scientific assertions, and five-figure
replacement. It was skipped, so it supplies no runtime claim.

Preflight:

```text
DSP_RADAR_VERIFY_PROFILE=contract python3 scripts/validate_curriculum.py
```

Exit 0 before implementation: `84 modules, 78 implemented`.

Authoring-focused validation:

```text
python3 -m py_compile tests/test_p79_module.py
DSP_RADAR_VERIFY_PROFILE=contract python3 scripts/validate_curriculum.py
python3 -m unittest tests.test_p79_module -v
```

Python compilation and curriculum validation passed with `84 modules, 79
implemented`. Twelve P79 tests passed, one actual-MATLAB test skipped, and the
evidence-presence test initially failed as designed before this retained file
existed. A first private-generator check also caught and corrected an
independently mistyped expected first value; the implementation generator was
unchanged.

Focused pre-evidence validation:

```text
DSP_RADAR_VERIFY_PROFILE=contract python3 scripts/validate_curriculum.py
python3 -m unittest \
  tests.test_p79_module.P79ModuleTests.test_manifest_identity_artifacts_and_permanent_dependency \
  tests.test_p79_module.P79ModuleTests.test_control_contract_accepts_baseline_and_rejects_malformed_resources \
  tests.test_p79_module.P79ModuleTests.test_independent_psf_oracle_matches_physical_limits_and_tradeoffs \
  tests.test_p79_module.P79ModuleTests.test_sparse_sampling_alias_failure_and_same_scene_recovery \
  tests.test_curriculum -v
git diff --check
```

Exit 0. Curriculum validation reported `84 modules, 79 implemented`; all ten
selected P79/curriculum tests passed in `1.129s`; whitespace validation passed.

Final focused validation:

```text
python3 -m py_compile tests/test_p79_module.py
DSP_RADAR_VERIFY_PROFILE=contract python3 scripts/validate_curriculum.py
python3 -m unittest tests.test_p79_module tests.test_curriculum -v
git diff --check
```

Exit 0. Curriculum validation reported `84 modules, 79 implemented`; 19 tests
ran in `1.632s`, with 18 passing and the one actual-MATLAB test explicitly
skipped. Python compilation and whitespace validation passed.

Contract quick validation:

```text
DSP_RADAR_VERIFY_PROFILE=quick python3 -m unittest discover -s tests -v
```

Exit 0 — all 1,284 discovered tests passed in `160.886s`; the P78 and P79
actual-MATLAB gates were the two explicit runtime-unavailable skips.

Full repository verification:

```text
DSP_RADAR_VERIFY_PROFILE=full ./scripts/agent-verify.sh
```

Exit 0. Curriculum validation reported `84 modules, 79 implemented`; all
1,284 discovered tests passed in `160.270s`, with the same two explicit
MATLAB-unavailable skips. The harness ran from
`2026-08-13T04:25:25+00:00` through `2026-08-13T04:28:05+00:00`; its ignored
local log is
`docs/evidence/local/verify-20260813-042525.log`.

Post-evidence focused and scope audit:

```text
python3 -m py_compile tests/test_p79_module.py
DSP_RADAR_VERIFY_PROFILE=contract python3 scripts/validate_curriculum.py
python3 -m unittest tests.test_p79_module -v
git diff --check
```

Exit 0. Curriculum validation again reported `84 modules, 79 implemented`;
13 P79 tests ran in `1.168s`, with 12 passing and the one actual-MATLAB test
explicitly skipped. Python compilation and whitespace validation passed. A
separate read-only EOF audit passed all 11 changed text files. The aggregate
SHA-256 over `.github`, `.codex`, `.agents`, and `contracts` remained
`b8cef5e2dcdda46029ed4caa5459aee47ff7f8e33aea55fb808ce7757d54476a`,
exactly matching pre-edit inspection and proving the operator-managed contract
activation plus all forbidden files were preserved.

Independent system-risk review found one medium-severity validation gap: the
permanent suite proved sparse replicas only on an isolated cross-range cut,
while the learner-facing broken-case claim is about false targets in the seeded
three-target 2-D image. The only test of that composite behavior was inside the
MATLAB subprocess gate, which is skipped in this environment. The in-scope
repair added
`test_composite_image_exposes_sparse_false_targets_and_exact_recovery`, a
bounded standard-library oracle that forms the complete dense, sparse, and
freshly recovered images. It binds both `+/-3 m` replicas, dense suppression,
deterministic image peaks, and exact dense recovery.

Independent review validation:

```text
python3 -m py_compile tests/test_p79_module.py
DSP_RADAR_VERIFY_PROFILE=contract python3 scripts/validate_curriculum.py
python3 -m unittest tests.test_p79_module tests.test_curriculum -v
git diff --check
DSP_RADAR_VERIFY_PROFILE=quick python3 -m unittest discover -s tests -v
DSP_RADAR_VERIFY_PROFILE=full ./scripts/agent-verify.sh
```

- Focused review: exit 0; curriculum validation reported `84 modules, 79
  implemented`; 20 tests ran in `1.790s`, with 19 passing and the P79
  actual-MATLAB test explicitly skipped.
- Quick repository suite: exit 0; 1,285 tests ran, with 1,283 passing and the
  P78 and P79 MATLAB-runtime gates explicitly skipped.
- Full repository verification: exit 0; curriculum validation passed and 1,285
  tests ran in `168.517s`, with 1,283 passing and the same two skips. The
  harness ran from `2026-08-13T04:34:10+00:00` through
  `2026-08-13T04:36:59+00:00`; its ignored local log is
  `docs/evidence/local/verify-20260813-043410.log`.
- Post-evidence focused rerun: exit 0; 20 tests ran in `1.424s`, with 19
  passing and the P79 MATLAB-runtime test skipped. Python compilation,
  curriculum validation, whitespace validation, and the exact-one-newline EOF
  audit over all 13 changed text files passed.

## Changed and preserved invariants

Changed within P79 ownership:

- completed the five files under
  `modules/79-compare-sar-resolution-aperture-length-and-windowing/`;
- changed only P79's manifest status to `implemented`, preserving its number,
  ID, title, question, phase, slug, folder, and batch identity;
- updated the root, start, and module catalogs for P79;
- added durable batch-local `tests/test_p79_module.py`, including permanent
  isolated-PSF and composite-image behavioral oracles;
- added this retained `docs/evidence/P79-2026-08-13.md`.

Preserved:

- exactly 84 unique modules in canonical order and a contiguous manifest-
  derived implementation prefix;
- permanent P78 prerequisite identity and all unrelated module content;
- future module statuses remain owned by their batches, and P79 tests accept
  later status advancement and unknown future metadata;
- personal learner state under ignored `.learning/` remains byte-for-byte
  isolated during temporary CLI tests;
- all forbidden contracts, workflows, skills, architecture/protocol files,
  build files, phase catalog, and unrelated tests;
- no external files, network calls, workers, timers, GPU/batch jobs, shell
  calls from MATLAB, commit, push, PR, merge, release, deployment, or settings
  changes.

The experiment's only failure is a bounded alternate spatial sampling grid.
Recovery uses the unchanged in-memory target scene, not an inverse applied to
the aliased image. There is no database, migration, service, external resource,
credential, persistent schema, background task, or backup/restore operation in
scope. Repository rollback is file-level and learner CLI rollback/recovery is
tested in isolated temporary fixtures.

## Residual risks

- Static review and the independent Python oracle cannot prove MATLAB parser,
  numeric, graphics, memory, performance, or version-specific behavior.
- The local separable broadside PSF omits wide-scene range/cross-range coupling,
  squint, migration residuals, motion error, propagation, clutter, calibration,
  antenna pattern, and range-dependent aperture support.
- Endpoint-inclusive discrete frequency and aperture sums approximate the
  continuous first-null formulas; reviewed tolerances bind the finite model.
- The three-point image is illustrative. Close-target complex interference is
  intentionally excluded from isolated width/PSL measurements.
- Educational effectiveness still requires a human learner observation and
  teach-back; repository automation cannot prove understanding.

## Rollback

Revert only the P79 module artifacts, batch-local test/evidence, public P79
catalog additions, and restore only P79's manifest status to `scaffolded`.
Preserve P78, future module entries, learner progress, operator-managed
contract activation changes, and every forbidden path. The experiment writes
no data, so there is no external backup, migration, service, or deployment to
reverse.

## Unperformed validation

- MATLAB or compatible runtime execution, MATLAB version/toolbox inventory,
  rendered-figure inspection, runtime assertions, runtime performance/memory,
  timeout/cancellation behavior, and interactive learner teach-back;
- target Quality CI or control-plane CI on a remote runner;
- hardware, HIL, bench capture, field, real-time/RT1/RT2, operational radar,
  environmental, calibration, wide-scene/squint, clutter, or motion-error
  validation;
- Unreal, signing, release packaging, staging, deployment, production,
  monitoring, or rollback drill.
