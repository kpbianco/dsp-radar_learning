# P83 retained evidence — 2026-08-14

## Claim boundary

P83 provides a deterministic, bounded, base-MATLAB source experiment and a
complete concept-first lesson package using synthetic moving-platform clutter,
array data, slow-time phase, and neighboring range snapshots. Independent
Python standard-library calculations regenerate the declared private samples
and exercise the steering, covariance, conventional filter, loaded joint
solve, maps, sweeps, contamination failure, and recovery as simulated numerical
evidence. Repository checks and source inspection are static.

Neither MATLAB nor Octave is installed. This evidence does not claim MATLAB
parsing, execution, repeatability, workspace behavior, or figure rendering. It
also does not claim measured resource use, physical radar/HIL, bench, field,
real-time, RT1/RT2, Unreal, operational clutter rejection, signing, release,
deployment, or production validation.

## Governance, current state, ownership, concurrency, and CI inspection

- Repository instructions, the complete module-builder skill, repository,
  profile, DSP/radar, active-batch, and verification contracts, canonical P83
  contract, named implementation/test areas, public catalogs, build file, and
  Quality workflow were read before implementation.
- Canonical product, PRD, acceptance, architecture, assumptions, decisions,
  repository ownership, risk, roadmap, traceability, handoff, automation,
  DRL-M9, P82 dependency, and P84 successor contract were inspected in the
  control worktree. No separate target ADR, safety, compatibility, CODEOWNERS,
  or current-state file exists; the inspected equivalents govern this batch.
- Branch `agent/p83` began at `627777f`, equal to `origin/main`, with P82
  implemented. A detached secondary worktree exists; no other worktree owns
  `agent/p83`.
- The only initial dirty path was the operator-managed transition of
  `contracts/active-batch.yaml` from P82 to P83. That forbidden path was read
  and preserved, not implementation-edited.
- One bounded read-only subagent audited the physical model and failure design.
  The principal agent retained all editing, integration, repair, and validation
  ownership.
- A later independent system-risk review read the full uncommitted diff,
  architecture, implementation/tutor contracts, verification harness, learner
  CLI, manifest schema, focused tests, retained claims, and module interfaces.
  It used no concurrent editor and preserved the operator-owned forbidden-path
  contract transition.
- Pre-edit GitHub inspection found the P82 pull-request Quality run
  `31763613702` successful and its main push run `31763762131` in progress; the
  latter subsequently completed successfully. P83 branch CI was not run because
  no commit, push, or pull request is allowed.

## Acceptance map

| Acceptance | Implementation and retained evidence |
|---|---|
| Guiding question | Exact canonical question appears in all five artifacts, manifest, and permanent tests. |
| Deterministic baseline | Seed 8301/8302 private Park-Miller complex samples, visible 4-element, 8-pulse, 48-range-cell controls, assertions, metrics, and five tagged figures. |
| Moving-platform scene | Forty-one ground patches follow `nu_c(theta)=0.30 sin(theta)` with explicit Kronecker space-time steering; the CUT target is near the ridge. |
| Fair processor comparison | Fixed beam plus Doppler bank and small STAP consume the same already range-compressed 4-by-8 snapshots and use dimensionless normalized-output map power. |
| Transparent adaptation | Guarded 36-cell sample covariance, trace-scaled diagonal loading, linear solve, distortionless normalization, and AMF map equation are exposed without a toolbox object or explicit inverse. |
| Baseline result | Independent oracle gives `-5.4322 dB` conventional and `+19.8249 dB` joint output SCNR; conventional target contrast is below 0 dB while STAP contrast exceeds 25 dB and the target owns only the adaptive map. |
| Sweep 1 | Target Doppler offset `[0.01 0.03 0.06 0.10]` from the local ridge exposes the identifiability limit and growing joint benefit. |
| Sweep 2 | Prefix support `[8 16 24 36]` from one unchanged clean record exposes finite-sample rank/conditioning and SCNR cost. |
| Broken case and recovery | Thirty-dB target-like contamination enters 25% of training; broken SCNR falls below `-3.65 dB`. The assumed look remains distortionless, desired-target output changes only `-0.62 dB`, and residual clutter-plus-noise rises `22.86 dB`; rebuilding from retained clean training and unchanged measurement exactly reproduces baseline data/weights/maps. |
| Lesson/walkthrough/checks | Physical equations, vector order/sign, limiting cases, one-transition guidance, 48 answered checks, and teach-back rubric. |
| Dependencies/compatibility | P37/P41/P42/P61/P63/P65/P68 concepts and governed P82 dependency declared; P84 identity is tested without constraining future status. Base MATLAB R2016b+, no toolbox/external data. |
| Negative/bounded behavior | Tests reject missing/empty/placeholder artifacts; boolean, nonfinite, noninteger, aliased, unsupported, guard-overlapping, unordered, and over-ceiling controls. Compute, storage, generator, map, solve, and figure ceilings are explicit. A behavioral regression test separates desired-response change from interference-output growth in the contamination failure. |
| Timeout/cancellation/rollback/isolation | Temporary CLI fixtures impose three seconds, simulate P83 scaffold rollback/P82 fallback, preserve repository learner state, and accept later P84 status/metadata. Foreground-only cancellation and scoped rerun behavior are documented and source-enforced. |
| Permanent tests | P83/P82 permanent facts are asserted; only P84 identity/batch is asserted. No later module is required to stay pending and no time-relative frontier claim appears in P83 tests. |
| Manifest transition | P83 becomes implemented with the complete artifact set and focused deterministic checks; P84 identity remains unchanged. |
| Honest evidence | Static, simulated, unavailable MATLAB runtime, physical, and release claim boundaries are separated. |

Product-data scoring, migration, database backup/restore, and service gates are
not applicable: P83 changes no score, schema, migration, database, or service.
Rollback is the relevant recoverability path.

## Deterministic simulated-oracle results

The Python oracle does not import or execute MATLAB source. It independently
regenerates MATLAB-column-ordered private complex samples and evaluates the
declared equations with standard-library complex arithmetic and pivoted linear
solves.

| Quantity | Result |
|---|---:|
| Elements / pulses / joint dimension | 4 / 8 / 32 |
| Range cells / CUT / guard | 48 / 25 / 2 each side |
| Clean training indices / count | 5–22 and 28–45 / 36 |
| Clutter patches / total clutter-to-noise | 41 / 32 dB |
| Ridge slope | 0.300000 cycles/pulse per `sin(angle)` |
| Actual target / assumed grid look | `(12.5 deg, 0.125)` / `(12 deg, 0.120)` |
| Conventional output SCNR | -5.4322352407 dB |
| Clean small-STAP output SCNR | +19.8249314039 dB |
| Clean joint improvement | 25.2571666446 dB |
| Conventional target/map-median contrast | -0.2261724882 dB |
| STAP target/map-median contrast | +25.5242407543 dB |
| Conventional global peak `(grid index, range index)` | `(41, 23)` zero-based, not target |
| STAP global peak `(grid index, range index)` | `(37, 24)` zero-based, target |
| Ridge sweep conventional SCNR | `[-6.9065, -6.5134, -5.2637, -2.3614] dB` |
| Ridge sweep STAP SCNR | `[-1.7547, 10.9456, 19.7361, 24.1673] dB` |
| Support sweep STAP SCNR | `[6.5462, 13.6985, 17.8881, 19.8249] dB` |
| Broken contaminated-training SCNR | -3.6516571502 dB |
| Broken desired-target output change from clean | -0.6152939520 dB |
| Broken clutter-plus-noise output increase from clean | +22.8612946021 dB |
| Broken target/map-median contrast | -0.1683857158 dB |
| Broken global peak `(grid index, range index)` | `(31, 45)` zero-based, not target |
| Clean-to-broken SCNR loss | 23.4765885541 dB |
| Recovered clean weight and measurement | element-for-element exact |

These are simulated equation results, not MATLAB results. The known analytical
clutter-plus-noise covariance is used only for the SCNR ruler; every adaptive
weight uses a sample covariance from the generated neighboring range cells.

## Figure and metric inventory

| Figure | Axes/metrics | Meaning |
|---|---|---|
| Angle-Doppler geometry | Broadside angle deg, normalized Doppler cycles/pulse, labeled clutter patch power dB/noise colorbar | Moving ground ridge and target offset. |
| Conventional versus STAP maps | Relative range cell, normalized Doppler cycles/pulse, labeled normalized-output-power dB colorbars on common limits | Same data through fixed separable and joint adaptive processing. |
| Target visibility and SCNR | Normalized Doppler, CUT/background-median dB; known-component output SCNR dB | One-realization visibility versus analytical simulated power accounting. |
| Ridge and support sweeps | Doppler offset cycles/pulse, SCNR dB; clean training cells, loaded condition number | Identifiability and finite-training costs. |
| Contamination and recovery | Relative range cell, normalized Doppler cycles/pulse, labeled normalized-output-power dB colorbars on common limits | Bad covariance loses interference rejection and hides the target score; retained clean record restores it. |

Map values and contrasts are dimensionless powers and use `10*log10`. SCNR is
also a power ratio in dB. Normalized Doppler converts to hertz by multiplying
by the 20 kHz PRF; no velocity conversion is claimed without a complete target
geometry.

## Resource, timeout, cancellation, and side-effect evidence

- Four retained maps are bounded at 51 Doppler cells by 48 range cells.
- The script executes 161 loaded 32-dimensional solves: three map banks plus
  four ridge and four support cases.
- The bounded dominant-work proxy is 5,799,424 multiply-accumulate equivalents
  under an 8,000,000 ceiling. It accounts for loaded solves, map projections,
  selected covariance products, and background formation; it is not an exact
  total or a measured operation count.
- The bounded source-array storage proxy is 30,824 numeric value equivalents
  under a 250,000 ceiling; the largest private-generator request is 3,936
  uniforms under 100,000. Graphics/runtime internals, copy-on-write behavior,
  and complex-value byte use are not measured.
- Elements, pulses, dimension, ranges, clutter patches, Doppler cells, training
  cells, sweep cases, maps, solves, private values, working values, and figures
  have separate immutable ceilings.
- CLI fixtures use a three-second subprocess timeout. Conditional MATLAB
  validation uses 300 seconds when an executable exists and is skipped here.
- Finite foreground loops perform no file/network I/O, device, timer, worker,
  GPU, shell, callback, checkpoint, persistent state, or asynchronous work.
  Ctrl+C cancellation is documented as a clean rerun but was not forced.

## Exact commands and results

Initial read-only inspection:

~~~text
git status --short --branch
git branch --show-current
git log --oneline --decorate -12
git worktree list --porcelain
git rev-list --left-right --count origin/main...HEAD
gh run list --workflow Quality --limit 5
command -v matlab
command -v octave
~~~

Result: branch `agent/p83` at `627777f`, 0/0 divergence from `origin/main`, no
competing branch worktree, successful latest completed P82 PR Quality run, and
no MATLAB/Octave executable. The operator-managed P83 contract activation was
the only initial dirty path.

~~~text
PYTHONDONTWRITEBYTECODE=1 DSP_RADAR_VERIFY_PROFILE=contract python3 scripts/validate_curriculum.py
PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tests.test_p83_module -v
python3 -m py_compile tests/test_p83_module.py
git diff --check
~~~

Result: curriculum contract validation passed with 84 modules and 83
implemented. After independent review repair, the P83 suite ran 15 tests: 14
passed and the conditional MATLAB runtime test skipped because MATLAB is
unavailable. Python compilation and `git diff --check` passed. The added
behavioral regression reproduces unit assumed-look response, `-0.6152939520 dB`
desired-target output change, and `+22.8612946021 dB` clutter-plus-noise output
growth in the broken case. An earlier red focused run had exposed the
not-yet-created evidence file, an unguarded zero denominator in the
malformed-control oracle, and an incorrect fixed generator fixture; all three
test-harness issues were repaired before the initial verification.

~~~text
PYTHONDONTWRITEBYTECODE=1 DSP_RADAR_VERIFY_PROFILE=quick python3 -m unittest discover -s tests -v
DSP_RADAR_VERIFY_PROFILE=full ./scripts/agent-verify.sh
~~~

Post-review quick-profile result: 1,344 tests ran in 168.002 seconds; 1,338
passed and six conditional MATLAB-runtime tests skipped. Post-review full
repository verifier result: curriculum validation passed at 84 modules/83
implemented, all 1,344 tests ran in 173.104 seconds with 1,338 passed and the
same six skips, and the verifier completed successfully at
`2026-08-14T03:15:22+00:00`. The retained full-run log is
`docs/evidence/local/verify-20260814-031229.log`.

## Changed and preserved invariants

Changed by P83:

- completed the P83 README and added experiment, lesson, walkthrough, checks;
- changed only the P83 manifest/index status from scaffolded to implemented;
- advanced manifest-derived public catalog prose/count/frontier from P82 to
  P83 while leaving P84 identity intact;
- added permanent P83 tests and this retained evidence.

Independent risk-review repair:

- corrected the broken-case explanation: the deterministic failure preserves
  the assumed distortionless look and loses SCNR primarily through a
  `22.86 dB` rise in clutter-plus-noise output, not through the previously
  claimed desired-target self-null;
- retained separate desired-output and interference-output metrics and added a
  MATLAB assertion that binds the demonstrated failure mechanism;
- added a standard-library numerical behavioral regression for the unit-look,
  desired-output, and interference-growth invariants;
- labeled all angle-Doppler and range-Doppler colorbars with their represented
  power quantity and dB units; and
- reran focused, quick, and full deterministic verification without changing
  the MATLAB/physical claim boundary.

Preserved:

- canonical 84-module identity, order, questions, folders, phases, and batches;
- P01–P82 implemented prerequisite prefix and P84 identity;
- P83 canonical experiment, procedure, learning goal, and completion condition;
- same array-pulse inputs for conventional and adaptive comparison;
- sample covariance for weights versus analytical covariance only for the
  simulated SCNR ruler;
- transparent base-MATLAB operations with no toolbox or external data;
- ignored learner progress and operator-owned batch activation;
- no implementation edits to forbidden paths beyond the preserved initial
  operator-managed contract activation;
- no commit, push, PR, merge, release, deployment, settings, hardware, or
  production action.

Tests enforce exactly one final newline and no extra EOF blank line for every
P83-changed text file.

## Residual risks

- MATLAB parsing, execution, assertions, figures, global-RNG isolation,
  repeatability, storage bounds, and measured runtime remain unvalidated.
- The independent Python oracle cannot prove identical MATLAB floating-point,
  `rank`, `cond`, graphics, local-function, or workspace behavior.
- The narrowband clutter ridge is an instructional side-looking model. It omits
  antenna patterns, range dependence, internal clutter motion, channel
  calibration, crab angle, acceleration, range ambiguity, waveform processing,
  jammers, CFAR, and detection thresholds.
- Thirty-six homogeneous training cells only narrowly exceed dimension 32;
  real secondary data may be correlated, heterogeneous, or target contaminated.
- The broken case intentionally combines contaminated training and a small
  steering mismatch. It demonstrates loss of interference rejection and
  normalized-map contrast; it does not demonstrate a deep desired-target null.
- Static resource equations are bounded planning proxies, not exhaustive or
  measured peak memory, CPU work, timeout, or cancellation latency.
- Human educational effectiveness was not playtested, and the unpushed branch
  has no P83 CI run.

## Rollback

Remove only the four added P83 module files, restore the P83 README scaffold,
remove the P83 test/evidence, restore only P83 manifest/index status to
scaffolded, and derive root catalog count/frontier prose from that state.
Preserve P82, P84 identity and any later P84 status/metadata, personal learner
state, the operator-managed active-batch contract, verification infrastructure,
and every forbidden path.

The runtime failure path mutates no external state. Rerunning closes only stale
P83-tagged figures and rebuilds deterministic private inputs.

## Unperformed validation

- MATLAB or compatible parsing/execution, repeat comparison, rendering,
  assertions, RNG/storage checks, and measured runtime
- forced Ctrl+C cancellation and wall-clock timing
- representative learner playtest or manual teach-back
- physical radar/target capture, hardware/HIL, bench, field, real-time,
  RT1/RT2, Unreal, or operational clutter validation
- signing, packaging, release, staging, deployment, production, or settings
- P83 GitHub Actions and external Control plane CI on this uncommitted/unpushed
  branch
- machine-readable `evidence.json`, because the active batch permits only
  `docs/evidence/P83-*.md` and no repository evidence-schema contract applies
- commit, push, pull request, merge, release, or deployment
