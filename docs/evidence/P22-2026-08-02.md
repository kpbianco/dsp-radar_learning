# P22 Retained Evidence — 2026-08-02

## Claim boundary

This record supports P22 repository structure, deterministic static contracts,
independent Python numerical checks, curriculum identity, and learner-harness
compatibility. MATLAB and Octave did not run. It does **not** claim rendered
figures, MATLAB-runtime numerical fidelity, hardware/HIL, bench, field,
RT1/RT2, real-time, operational radar, Unreal, signing, staging, production,
release, deployment, or human-learning validation.

`matlab` and `octave` were absent from `PATH`. The repository Quality workflow
uses Python 3.12 and `./scripts/agent-verify.sh`; it has no licensed MATLAB
runtime. Read-only GitHub inspection returned no P22 pull request or branch CI
run. No commit, push, PR, merge, release, deployment, or settings change was
performed.

## Governing inputs and repository state

- Branch: `agent/p22` at starting commit `ec8f302`, the merged P21 mainline.
- Dependency: P21 was `implemented` before P22 work began.
- Worktrees/concurrency: one registered worktree; no competing P22 worktree,
  pull request, Actions run, or separate implementation process was found.
- The pre-existing `contracts/active-batch.yaml` modification activates P22.
  It is operator/control-plane state in a forbidden path and was preserved,
  byte-for-byte during implementation, not authored or altered by this batch.
- Active and canonical P22 contracts matched. The declared older baseline
  `10069b5d` is an ancestor of current HEAD rather than a conflicting state.
- No repository CODEOWNERS, repository current-state file, P22-specific ADR,
  safety record, or compatibility record exists beyond the inspected
  governance, contracts, history, Quality workflow, module brief, and canonical
  product handoff/control artifacts.
- Two bounded read-only design/pattern audits and two final read-only
  implementation/scope reviews were assigned. The principal agent retained all
  edit and integration ownership.

## Acceptance mapping

| Acceptance item | Implemented evidence | Validation class |
| --- | --- | --- |
| Preserve the guiding question | Exact canonical question appears in all five P22 artifacts and unchanged identity fields | Static |
| Deterministic experiment, visible parameters, labeled plots/metrics, sweeps, and broken case | `experiment.m`: private seed 1022, 4800 samples at 24 ksample/s, 3 kHz carrier, 100 Hz message, 400 Hz deviation, explicit controls, five P22 figure groups, deviation and message-frequency sweeps, and an intentional Nyquist violation | Static plus independent Python model |
| Explain DSP/radar model and limiting cases | `lesson.md`: explicit FM phase/instantaneous-frequency equations, constant phasor magnitude, Bessel-like sideband ladder, 98% line-power width, Carson estimate, limiting cases, sampling limit, and radar connection | Static/manual-content review |
| Guide baseline, one-variable changes, failure interpretation, and connection | `walkthrough.md`: time/phase baseline, spectrum baseline, deviation-only sweep, message-frequency-only sweep, aliased failure, recovery, and prerequisite connections | Static/manual-content review |
| Test interpretation and prediction | `checks.md`: frequency/sideband prediction, deviation/message-rate interpretation, false-statement corrections, Nyquist diagnosis/recovery, resource checks, and teach-back rubric | Static/manual-content review |
| Declare dependencies and avoid black boxes | P21 is the immediate prerequisite; P11-P13 and P16 are conceptual links; FM phase, phase derivative, FFT normalization, line-power accumulation, and aliasing are explicit base-MATLAB operations | Static |
| Mark implemented only after required files/checks pass | Five nonempty artifacts, exact P22 identity/status, P21 prerequisite, public catalog, manifest frontier, and isolated CLI lifecycle behavior are tested | Static |
| Separate static from MATLAB runtime evidence | This claim boundary and unperformed-validation inventory explicitly state that MATLAB/Octave did not run | Static evidence policy |

## Experiment and figure/metric inventory

All axes and metrics identify units or explicitly state dimensionless scales.

1. **FM motion in time:** normalized 100 Hz message; designed and
   finite-difference instantaneous frequency in hertz versus milliseconds;
   transmitted complex-phasor magnitude in volts; clean and seeded-noisy RF
   voltage in volts; fixed 3 kHz carrier and +/-400 Hz deviation.
2. **FM spectrum and bandwidth:** positive RF frequency in hertz; amplitude in
   dB re 1 V; 3 kHz carrier; Carson boundaries; sideband order at
   `fc+n*fm`; fraction of retained clean line power; smallest symmetric 98%
   order and bandwidth in hertz.
3. **Deviation-only sweep:** `Delta_f=[50,200,400,800] Hz`; fixed 100 Hz
   message, carrier, amplitude, record, sampling, and line-power rule; each RF
   spectrum labels dimensionless beta and `B98` in hertz; retained `B98` and
   Carson-width vectors are in hertz.
4. **Message-frequency-only sweep:** `fm=[50,100,200,400] Hz`; fixed 400 Hz
   deviation, carrier, amplitude, record, sampling, and line-power rule; each RF
   spectrum labels dimensionless beta and `B98` in hertz; retained `B98` and
   Carson-width vectors are in hertz.
5. **Nyquist failure/recovery:** intended and observed instantaneous frequency
   in hertz versus milliseconds; 12 kHz Nyquist reference; sampled signed
   frequency in hertz; magnitude in dB re 1 V; negative Nyquist margin in
   hertz; aliased-sample fraction; 27.2 ksample/s guarded Carson/98%-target
   minimum; and bounded 30 ksample/s recovery with unwrapped phase increments,
   a 10.2 kHz `B98` through order `+/-51`, and 1.9 kHz upper-edge margin.

Retained `results` include seed/sampling/carrier/message/deviation controls,
time/message/phase/frequency/RF arrays, finite-difference frequency, positive
RF spectrum, 98%-line-power and Carson metrics, both sweep controls and width
vectors, phasor/frequency errors, intended/observed broken frequency, Nyquist
margin, aliased fraction, recovered trace/error, and minimum/demonstrated
recovery sample rates, plus the broken-case Carson edge and recovered 98%
sideband order, bandwidth, and upper-edge margin.

## Independent numerical and contract coverage

`tests/test_p22_module.py` uses only Python's standard library and does not
execute or translate MATLAB. It checks:

- exact artifact, manifest identity, P21 prerequisite, and catalog contracts,
  plus missing, empty, duplicate, non-list, and wrong-identity negatives;
- fixed visible controls, a private seeded noise stream, and no global RNG;
- an independent complex-phasor construction proving 1 V magnitude and the
  exact `fc +/- Delta_f` instantaneous-frequency limits;
- an independent finite Fourier coefficient model for beta 4, showing more
  than seven appreciable lines, 98% power at order +/-5, and a 100 Hz `B98`
  equal to the one-tone Carson estimate in the scaled check;
- independent four-case deviation and message-frequency sweep widths, plus
  source binding that each loop changes only its named physical input;
- the negative Nyquist case: 13 kHz intended maximum above 12 kHz Nyquist,
  invalid sampled bandwidth, 27.2 ksample/s guarded Carson/98%-target minimum,
  and 30 ksample/s phase-increment recovery;
- an independent occupied-line-power recovery behavior showing that the 30
  ksample/s record retains at least 98% through order `+/-51`, for a 10.2 kHz
  width and 13.1 kHz upper edge with 1.9 kHz Nyquist margin;
- logical, nonfinite, complex, noncanonical, malformed-vector, out-of-band,
  and expanded resource-control rejection before random/allocation/FFT/cleanup/
  figure work;
- five fixed figure groups, units, result inventory, 4800-sample baseline,
  6000-sample recovery, four-case, five-figure, and 650000-value bounds;
- placeholder/opaque-toolbox/external-I/O rejection; cancellation, recovery,
  figure/RNG/learner-state isolation, compatibility, rollback, retained-
  evidence, and runtime-overclaim contracts.

These tests assert permanent facts about P22 and prerequisite P21. They do not
require a later module status or call P22 permanently latest. Shared curriculum
tests derive the moving frontier from the manifest and simulate a later valid
transition.

## Exact commands and results

### Before editing

```text
git status --short --branch; git branch --show-current; git log -8 --oneline --decorate
PASS — branch agent/p22 at ec8f302; only contracts/active-batch.yaml was
modified by the operator/control-plane activation.

git worktree list --porcelain; git remote -v; git branch -vv
PASS — one registered worktree; local/remote branch ownership and merged P21
base inspected; no competing P22 worktree.

DSP_RADAR_VERIFY_PROFILE=contract python3 scripts/validate_curriculum.py
PASS — Curriculum validation passed: 84 modules, 21 implemented.

DSP_RADAR_VERIFY_PROFILE=quick python3 -m unittest tests.test_curriculum tests.test_learn_cli tests.test_p21_module -v
PASS — Ran 102 tests in 12.833s; OK.

command -v matlab; command -v octave
RESULT — matlab absent; octave absent.

gh pr list --head agent/p22 --state all --json number,state,title,headRefName,baseRefName,statusCheckRollup,url
PASS (read-only) — returned `[]`.

gh run list --branch agent/p22 --limit 10 --json databaseId,status,conclusion,workflowName,headBranch,headSha,url,createdAt
PASS (read-only) — returned `[]`.
```

### Focused implementation iterations

```text
DSP_RADAR_VERIFY_PROFILE=contract python3 scripts/validate_curriculum.py
PASS — Curriculum validation passed: 84 modules, 22 implemented.

python3 -m unittest discover -s tests -p 'test_p22_module.py' -v
EXPECTED ITERATION FAILURE — Ran 14 tests in 0.044s; three failures covered the
not-yet-created evidence file and two overly strict textual count/line-wrap
assertions. All independent FM numerical, malformed-input, resource, scope,
and source-contract checks passed. The two test-only assertions were corrected.

python3 -m unittest discover -s tests -p 'test_curriculum.py' -v
PASS — Ran 6 tests in 0.102s; OK.

python3 -m unittest tests.test_learn_cli.LearnCliTests.test_start_p22_module tests.test_learn_cli.LearnCliTests.test_default_start_does_not_skip_p21_after_p22_becomes_implemented tests.test_learn_cli.LearnCliTests.test_default_start_advances_to_p22_after_p21_completion tests.test_learn_cli.LearnCliTests.test_complete_p22_persists_current_completion_and_note -v
PASS — Ran 4 tests in 0.833s; OK in isolated temporary repositories.

python3 -m unittest discover -s tests -p 'test_p22_module.py' -v
EXPECTED ITERATION FAILURE — Ran 14 tests in 0.147s; only the evidence file and
one remaining line-wrap-sensitive recovery phrase failed. All FM numerical and
behavioral checks passed. The phrase test was corrected before this file was
written.
```

### Pre-system-review local verification

```text
DSP_RADAR_VERIFY_PROFILE=contract python3 scripts/validate_curriculum.py
PASS — Curriculum validation passed: 84 modules, 22 implemented.

python3 -m unittest discover -s tests -p 'test_p22_module.py' -v
PASS — Ran 14 tests in 0.147s; OK after all review repairs.

python3 -m unittest discover -s tests -p 'test_curriculum.py' -v
PASS — Ran 6 tests in 0.067s; OK.

python3 -m unittest discover -s tests -p 'test_learn_cli.py' -v
PASS — Ran 83 tests in 13.619s; OK in isolated temporary repositories.

DSP_RADAR_VERIFY_PROFILE=quick python3 -m unittest discover -s tests -v
PASS — Ran 406 tests in 26.227s; OK.

DSP_RADAR_VERIFY_PROFILE=full ./scripts/agent-verify.sh
PASS — Curriculum validation passed with 84 modules and 22 implemented;
Ran 406 tests in 25.582s; OK. Ignored local log:
docs/evidence/local/verify-20260802-164722.log.

git diff --check
PASS — no whitespace errors.
```

The canonical batch names Control plane validation and target Quality
validation as required CI. No P22 PR or remote branch run exists, so those
remote CI checks were not performed and are not claimed. The equivalent target
Quality command was executed locally by the full verifier above.

## Independent reviews and repairs

Four bounded read-only audits covered prior implementation conventions,
scientific design, MATLAB/DSP correctness, and system/governance risk. The
reviews confirmed the FM phase law, bin alignment, 98%-line-power method,
one-variable sweeps, canonical progression, and allowed-path ownership. They
also found four implementation defects, all repaired before the final results:

1. `xline`/`yline` convenience calls conflicted with the repository's older
   base-MATLAB compatibility convention. All reference lines now use ordinary
   two-point `plot` calls, and the regression test rejects both calls.
2. The first recovery text recommended 30 ksample/s while fixed controls could
   not execute that alternative. The bounded script now constructs the same
   broken phase law at 30 ksample/s with 6000 samples, asserts its phase-slope
   margin and phase-increment error, and overlays the recovered trace.
3. Source-only checks could miss a wrong sweep or non-aliasing broken trace.
   MATLAB assertions now bind exact deterministic sweep bandwidth vectors and
   an alias fraction above 0.15; independent Python checks reproduce both
   sweep vectors, the aliased phase increments, and the clean recovery.
4. Cancellation before the old `results` reset could expose stale results from
   a prior run. `results` now resets immediately after all guards and before
   random/signal work; documentation accurately states that a script still
   mutates caller-workspace variables and cannot restore overwritten names.

Static review also corrected the claim boundary: timeout, cancellation,
rerun recovery, and rollback behavior were reasoned from source and prose, not
executed in MATLAB. No other material equation, unit, resource, compatibility,
scope, lifecycle, permanence, or claim-boundary defect remained.

## Independent system-risk review and repair

The full uncommitted P22 diff was subsequently reviewed against `AGENTS.md`,
the active-batch, product-profile, and DSP-radar contracts, architecture,
implementation and tutor contracts, curriculum and learner-CLI public behavior,
module equations, tests, recovery boundaries, licensing/dependency claims, and
retained evidence. The review covered correctness, concurrency, lifetime,
resource, migration, compatibility, security, licensing, recovery, and missing-
test failure modes.

One medium-severity conceptual recovery defect was found. The broken-case
minimum sample rate used only the 13 kHz maximum instantaneous frequency plus a
500 Hz guard, producing 27.0 ksample/s. That criterion omitted the lesson's own
finite occupied-band target: for this one-tone case the Carson/98% upper edge is
`fc + Delta_f + fm = 13.1 kHz`. The old calculation therefore supplied only
400 Hz, not the claimed 500 Hz, beyond that target. It also allowed “recovery”
wording to be read as perfectly alias-free even though ideal sinusoidal FM has
infinitely many nonzero sidebands.

The repair changes the guarded minimum to 27.2 ksample/s, keeps the bounded 30
ksample/s demonstration, explicitly measures its 98%-line-power cluster, and
retains the resulting order `+/-51`, 10.2 kHz width, and 1.9 kHz upper-edge
margin. Lesson, walkthrough, checks, metrics, and claim language now distinguish
unwrapped phase increments and the named finite-power target from a strictly
bandlimited waveform. The missing behavioral regression independently builds
the 30 ksample/s FM record, proves the 98% occupied cluster and margin, and binds
those results to the MATLAB source assertions. No MATLAB or Octave runtime was
available, so this remains independent Python numerical and static evidence.

Exact post-review results:

```text
python3 -m unittest discover -s tests -p 'test_p22_module.py' -v
PASS — Ran 15 tests in 0.569s; OK after the retained-evidence update.

DSP_RADAR_VERIFY_PROFILE=contract python3 scripts/validate_curriculum.py
PASS — Curriculum validation passed: 84 modules, 22 implemented.

DSP_RADAR_VERIFY_PROFILE=quick python3 -m unittest discover -s tests -v
PASS — Ran 407 tests in 26.470s; OK.

./scripts/agent-verify.sh
PASS — Curriculum validation passed with 84 modules and 22 implemented;
Ran 407 tests in 26.068s; OK. Ignored local log:
docs/evidence/local/verify-20260802-165811.log.

git diff --check
PASS — no whitespace errors.
```

## Changed and preserved invariants

Changed within P22 ownership:

- completed `modules/22-relate-fm-deviation-to-bandwidth/` with the experiment,
  lesson, walkthrough, checks, and implemented README;
- changed only P22's manifest/module-index status to `implemented`;
- added P22 public catalog text, P22 learner-lifecycle coverage in the shared
  CLI test, one permanent P22 module test, and this retained evidence file.

Preserved:

- exactly 84 unique ordered module identities and nine canonical phases;
- canonical P22 title, question, experiment/procedure, learning goal,
  completion condition, and P21 dependency;
- implemented P01-P21 content/status and all later canonical identities;
- ignored `.learning/` personal state remains uncommitted and outside all
  repository tests;
- all forbidden paths, including operator-owned active-batch content;
- base-MATLAB/no-hidden-toolbox, private-RNG, finite-resource, bounded-runtime,
  and no-external-write boundaries.

## Rollback and recovery

Rollback removes P22-owned artifacts, P22 tests/evidence, and allowed P22
catalog/lifecycle text, then restores only P22's manifest/module-index status to
`scaffolded`. It preserves implemented P21, later canonical identities,
`.learning/` state, managed contracts, workflows, verification infrastructure,
and operator activation state.

At runtime, invalid controls fail before cleanup or allocation. As a MATLAB
script, P22 assigns variables in the caller workspace. Ctrl+C after validation
may leave those variables, partial P22 figures, and empty/incomplete `results`,
but no file, worker, timer, device, network, or external transaction. A full
rerun repeats the private seed and replaces P22 variables/figures/results, but
cannot restore a pre-existing caller variable that used the same name. The
intentional aliased samples cannot be repaired after capture; recovery raises
the sample rate or lowers carrier/deviation before a new acquisition.

## Profile gate applicability

The applicable product-data gates are curriculum schema/canonical identity,
uniqueness/completeness, deterministic invariants, recoverability/rollback, and
learner service smoke behavior. P22 has no scoring model, database/schema
migration, persistent canonical data service, production backup, or restore
target. Scoring-invariant, migration execution, database rollback,
backup/restore, and production service gates are therefore not applicable; no
fictitious migration or service evidence is claimed.

The generic `verify-change` workflow requests a schema-conforming
`evidence.json`, but P22's approved scope permits only `docs/evidence/P22-*.md`
evidence artifacts. No JSON artifact was added outside the active-batch path
contract; this approved Markdown record contains the acceptance map, commands,
residual risks, and unperformed-validation inventory instead.

## Residual risks and unperformed validation

- No MATLAB/Octave runtime or plot rendering occurred. MATLAB syntax,
  `RandStream` values, assertions, figure layout, and exact seed-1022 noisy
  measurements remain unconfirmed in the named runtime.
- Timeout, cancellation, rerun recovery, and rollback are covered by static
  source/text contracts only; no MATLAB process was interrupted or recovered.
- Static and independent Python checks do not prove educational effectiveness;
  no learner teach-back or manual completion was performed.
- The baseline and sweep messages are bin-centered in a rectangular 0.20 s
  record so exact line-power measurement is visible. Off-bin or nonperiodic
  messages leak across FFT bins and require a more general occupied-power
  integration/windowing method.
- The 98% rule sums ideal clean tone bins, not noisy adjacent bins or a
  regulatory emission mask. Carson's rule is an estimate, not a hard edge.
- The finite-difference frequency trace is a one-sample approximation of the
  known phase law. A real receiver needs analytic/baseband phase estimation and
  becomes sensitive to noise and low magnitude.
- The valid experiments keep the named Carson/98%-occupied target plus guard
  below Nyquist. Ideal sinusoidal FM is not strictly bandlimited, and practical
  anti-alias filters need a specified emission target and wider transition-band
  guard than this teaching check.
- Hardware/HIL, bench, field, RT1/RT2, real-time, operational radar, Unreal,
  signing, staging, production, release, deployment, remote CI, and human-
  learning validation were not performed and are not claimed.
