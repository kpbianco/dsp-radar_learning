# P78 retained implementation evidence — 2026-08-13

## Claim boundary

P78 implements the complete repository artifacts and deterministic contracts
for **Observe and Correct Range-Cell Migration**. Evidence here covers static
repository validation, bounded Python scientific oracles, learner-CLI behavior,
and retained source/document review. No `matlab`, `octave`, or `octave-cli`
executable was available, so no MATLAB script execution, rendered-figure
inspection, MATLAB version/toolbox result, or MATLAB timeout/cancellation result
is claimed.

This synthetic result is not physical hardware/HIL, bench, field, RT1/RT2,
real-time, operational radar, Unreal, signing, staging, release, deployment, or
production validation.

## Governance, current state, ownership, concurrency, and CI inspection

Before editing, branch `agent/p78` pointed to `e3983fb`, the merged P77 commit
and `origin/main`. P77 was implemented and satisfied P78's dependency. The
canonical baseline named by the batch, `10069b5`, is historical product
provenance; implementation continued from the newer integrated P77 state.

The registered target worktree was the only branch worktree. A separate
detached repository worktree was observed and untouched. `git ls-files -u`
reported no index conflicts, and no index lock or competing P78 implementation
was found. No repository `CODEOWNERS`, P78-specific ADR, separate safety rule,
or compatibility record exists beyond the inspected governance, architecture,
implementation/tutor protocols, decision/risk records, dependency modules, and
tests. The principal agent retained all edit and integration ownership. Two
bounded subagents performed read-only physics and durable-test audits and made
no file changes.

The only pre-existing dirty paths were operator-managed P78 activation changes
in forbidden `contracts/active-batch.yaml` and `contracts/repo-profile.yaml`.
They were read and preserved, not authored or modified by this implementation.
CI is `.github/workflows/quality.yml`: Ubuntu, Python 3.12, a ten-minute job,
and `./scripts/agent-verify.sh`. The CI workflow and all other forbidden paths
were preserved.

## Acceptance map

| Acceptance item | Implemented evidence | Validation class |
| --- | --- | --- |
| Preserve the guiding question | Exact question appears in all five module artifacts and the immutable manifest identity remains unchanged. | Static test and curriculum validation |
| Deterministic baseline, visible controls, labeled plots/metrics, sweeps, broken case | `experiment.m` uses private seed `7801`, explicit physical controls, six labeled figure groups, aperture-length and squint sweeps, fixed-bin comparison, wrong-sign failure, and retained metrics. | Static contract plus simulated Python oracle |
| Explain the model and limiting cases without syntax-first teaching | `lesson.md` connects exact square-root slant range, round-trip delay, compressed magnitude, two-way phase, interpolation, stored bins versus resolution cells, and nine limiting cases. | Static/manual content review |
| Guide baseline, one-variable changes, failure interpretation, and connection | `walkthrough.md` sequences geometry, row alignment, fair fixed/corrected comparison, two one-variable sweeps, image completion, wrong-sign failure, recovery, cancellation, rollback, and P76–P80 connections. | Static/manual content review |
| Test interpretation and prediction | `checks.md` provides 39 answered checks, completion checklist, and short teach-back rubric. | Static test/manual learning gate |
| Declare dependencies and expose essential behavior | README declares P18/P30/P32/P37/P75/P76/P77 and base MATLAB R2016b; source exposes exact range, linear interpolation weights, two-way compensation, and coherent sum with no opaque toolbox call. | Static regression test |
| Set implemented status only after required files/checks | All five artifacts existed and focused curriculum/scientific checks passed before `status: implemented` was treated as validated. | Curriculum and focused tests |
| Separate static from MATLAB runtime evidence | This evidence and all module prose explicitly state runtime unavailability and claim limits. | Evidence-content test |

## Deterministic simulated-oracle results

The permanent Python oracle independently reconstructs the MATLAB equations
using only the standard library, including MATLAB column-major placement of the
private seeded complex noise. It uses the same complex history and same exact
two-way phase compensation in fixed-bin and corrected paths; only range
sampling differs.

| Oracle metric | Result | Permanent criterion |
| --- | ---: | ---: |
| Exact geometric range span | `33.2473082472 m` | visible long-aperture migration |
| Stored-bin span from geometry | `66.4946164943` bins at `0.5 m/bin` | greater than 40 bins |
| Physical resolution-cell span | `16.6236541236` cells at `2 m/cell` | distinguishes bins from resolution |
| Seeded measured ridge span | `33.5 m` | follows exact path within sampling |
| Corrected ridge span | `0.5 m` | at most `1.0 m` |
| Wrong-sign ridge span | `66.5 m` | greater than `1.8` times uncorrected |
| Fixed/correct/wrong coherent peaks | `436.3967693 / 1548.0803721 / 307.2823324` | correct exceeds fixed and wrong |
| Corrected/fixed peak gain | `3.5474148324` | greater than `3.0` |
| Corrected/fixed power-concentration gain | `1.6411301260` | greater than `1.4` |
| Fixed/corrected image true-pixel ratio | `0.2184656084` | less than `0.40` |
| Corrected/fixed image concentration gain | `2.3819839452` | greater than `1.8` |
| Corrected image peak coordinate | `(60 m, 1000 m)` | exact reviewed image-grid truth |
| Aperture `100/200/400 m` spans | `5.98181 / 12.71911 / 33.24731 m` | strictly increasing |
| Squint `0/60/80 m` spans | `19.80390 / 33.24731 / 38.46040 m` | strictly increasing |
| Maximum reviewed adjacent two-way phase step | `2.8223890245 rad` | every sweep case below `0.9*pi` |
| Correct-sign fresh recovery | exact matrix and profile equality | unchanged-input recovery |

The profile concentration is a reviewed-gate regression metric: because its
denominator includes the retained range window and noise-only columns, it is
not presented as a geometry-only physical constant. Ridge span, coherent peak
gain, and same-grid image behavior carry the stronger physical claims.

The oracle also covers malformed/nested/Boolean/nonfinite/unordered controls,
invalid seed and negative noise, off-grid/nonuniform axes, inadequate support,
spatial phase aliasing, target/grid mismatch, interpolation endpoints, and
aperture/range/image/sweep/private-noise/interpolation/image/total/live-storage/
figure ceilings. Transparent complex interpolation is checked on a fractional
sample, and the private generator is checked for repeatability, seed isolation,
known first value, and bounded count rejection.

Reviewed scheduled correction-and-image work is `1,157,523`
row-interpolation requests plus `3,557,422`
pixel-look requests, totaling `4,714,945`. This includes the first correct
alignment, wrong-sign failure, fresh correct recovery, fixed-bin image, and
path-following image. Predicted and executed counters must match exactly.

## Figure and metric inventory

1. **Geometry and observed migration:** platform and target coordinates (m);
   normalized range-history magnitude (dB) versus slant range (m) and platform
   cross-range (m); exact modeled path; true/measured migration (m and bins).
2. **Before/after correction:** uncorrected and reference-aligned magnitude
   matrices (dB) versus range and platform position (m); measured ridge spans
   (m).
3. **Fair coherent profile comparison:** fixed-bin and path-following profiles
   (dB) versus aligned slant range (m); coherent peak and power-concentration
   gains.
4. **Two controlled geometry sweeps:** center-referenced range change (m) versus
   platform position (m) for aperture lengths `100/200/400 m` and squint
   offsets `0/60/80 m`; migration spans in metres and stored bins.
5. **Completion image comparison:** fixed-bin and path-following range/cross-
   range images (dB) with truth marker; true-pixel voltage ratio, peak-power
   concentration gain, and focused coordinate (m).
6. **Wrong-sign failure and recovery:** uncorrected/wrong/recovered ridge range
   (m) versus platform position (m); wrong/recovered coherent profiles (dB);
   exact unchanged-data and recovery flags.

Immutable ceilings are 2,001 aperture samples, 251 range samples, 1,500 image
pixels, five cases per sweep, 800,000 private-generator values, 1,200,000
interpolation operations, 4,000,000 image operations, 5,200,000 scheduled
correction-and-image operations, 12,000,000 eight-byte live-value equivalents, and six tagged
figure groups.

## Exact commands and results

Runtime discovery:

```text
command -v matlab
command -v octave
command -v octave-cli
```

All returned no executable and no output. No MATLAB-compatible runtime result
was produced.

First authoring-focused run:

```text
python3 -m unittest tests.test_p78_module -v
```

The initial run correctly failed because the retained evidence file did not yet
exist, the exact question was line-wrapped in two documents, and extending the
range gate by 5 m changed the expected power-concentration oracle by
`0.0002182` through additional noise-only columns. The artifact wording and
oracle expectation were corrected. A prior preflight run also caught that the
wrong-sign response plus response-width margin extended 0.49 m past the
original 1070 m gate; the gate was expanded to 1075 m so truncation cannot fake
concentration.

Authoring read-only audits found that the original `180 m` squint case exceeded the
declared adjacent phase-step guard and that the 2-D completion image was only
protected by unexecuted MATLAB assertions. The supported third squint case is
now `80 m`, every reviewed sweep path is preflighted below `0.9*pi`, and the
permanent standard-library oracle now forms both complete images and binds the
true-pixel ratio, concentration gain, and focused coordinate. Negative helper
tests also cover invalid shift sign/path, final interpolation endpoint, and
malformed image hypotheses.

Focused pre-evidence validation:

```text
python3 -m py_compile tests/test_p78_module.py
DSP_RADAR_VERIFY_PROFILE=contract python3 scripts/validate_curriculum.py
python3 -m unittest \
  tests.test_p78_module.P78ModuleTests.test_manifest_identity_artifacts_and_permanent_dependency \
  tests.test_p78_module.P78ModuleTests.test_control_contract_accepts_baseline_and_rejects_malformed_resources \
  tests.test_p78_module.P78ModuleTests.test_seeded_profile_oracle_isolates_migration_and_exact_recovery -v
```

Exit 0. Curriculum validation reported `84 modules, 78 implemented`; all three
selected artifact, malformed/resource, and scientific-oracle tests passed in
`25.743s`.

Focused final validation:

```text
python3 -m py_compile tests/test_p78_module.py
DSP_RADAR_VERIFY_PROFILE=contract python3 scripts/validate_curriculum.py
python3 -m unittest tests.test_p78_module tests.test_curriculum -v
git diff --check
```

Exit 0. After final audit repairs, curriculum validation reported `84 modules,
78 implemented`; all 12 P78 tests and six shared curriculum tests passed in
`112.076s`; Python compile and whitespace validation passed. The longer focused
duration reflects the retained full 2-D fixed/path image oracle.

Contract quick validation:

```text
DSP_RADAR_VERIFY_PROFILE=quick python3 -m unittest discover -s tests -v
```

Exit 0 — all 1,270 discovered tests passed in `167.142s`.

Full repository verification:

```text
DSP_RADAR_VERIFY_PROFILE=full ./scripts/agent-verify.sh
```

Exit 0. Curriculum validation reported `84 modules, 78 implemented`; all
1,270 discovered tests passed in `162.582s`. The harness ran from
`2026-08-13T03:42:02+00:00` through `2026-08-13T03:44:45+00:00`; its ignored
local log is
`docs/evidence/local/verify-20260813-034202.log`.

Independent system-risk review added a missing implementation-level behavioral
test. `test_actual_matlab_script_is_repeatable_and_bounded_when_available`
invokes the actual `experiment.m` in an isolated MATLAB subprocess when a
runtime is installed; it checks the public `p78_results` failure/recovery
metrics, caller RNG isolation, exact deterministic rerun, and replacement of
the first six tagged figures by exactly six second-run figures. The subprocess
has a five-minute timeout. In this review environment the test was skipped
because no `matlab` executable was available, so it supplies a future runtime
gate but no new MATLAB execution claim.

Independent review validation:

```text
python3 -m unittest tests.test_p78_module -v
DSP_RADAR_VERIFY_PROFILE=contract python3 scripts/validate_curriculum.py
DSP_RADAR_VERIFY_PROFILE=quick python3 -m unittest discover -s tests -v
DSP_RADAR_VERIFY_PROFILE=full ./scripts/agent-verify.sh
git diff --check
```

Results are recorded after the commands complete below.

- Focused P78: exit 0; 13 tests ran in `115.366s`, with 12 passing and the
  actual-MATLAB test explicitly skipped because no executable was available.
- Contract curriculum validation: exit 0; `84 modules, 78 implemented`.
- Quick repository suite: exit 0; 1,271 tests ran in `155.524s`, with one
  explicit MATLAB-unavailable skip.
- Full `agent-verify.sh`: exit 0; curriculum validation passed and 1,271 tests
  ran in `157.363s`, with the same one explicit skip. The harness ran from
  `2026-08-13T03:55:26+00:00` through `2026-08-13T03:58:04+00:00`; its ignored
  local log is `docs/evidence/local/verify-20260813-035526.log`.
- Python compile and `git diff --check`: exit 0.

## Changed and preserved invariants

Changed within P78 ownership:

- completed the five files under
  `modules/78-observe-and-correct-range-cell-migration/`;
- changed only P78's manifest status to `implemented` while preserving its
  number, ID, title, question, phase, slug, folder, and batch identity;
- updated the root, start, and module catalogs for P78;
- added the durable batch-local `tests/test_p78_module.py`;
- added this retained `docs/evidence/P78-2026-08-13.md`.

Preserved:

- exactly 84 unique modules in canonical order and a contiguous manifest-
  derived implementation prefix;
- permanent P77 prerequisite identity and all unrelated module content;
- future module statuses remain owned by their own batches, and P78 tests
  accept later status advancement and unknown future metadata;
- personal learner state under ignored `.learning/` remained byte-for-byte
  isolated during temporary CLI tests;
- all forbidden contracts, workflows, skills, architecture/protocol files,
  build files, phase catalog, and unrelated tests;
- no external files, network calls, workers, timers, GPU/batch jobs, shell
  calls, commit, push, PR, merge, release, deployment, or settings changes.

CLI compatibility uses a bounded three-second subprocess timeout. Explicit P78
start succeeds with simulated later metadata; P78 rollback is refused for
explicit tutoring; restoration succeeds; and default selection falls back to
P77 across a deliberately invalid future status gap because only the contiguous
implemented prefix is selectable. The temporary fixture does not alter the
repository's learner state.

## Residual risks

- Static review and an independent Python oracle cannot prove MATLAB parser,
  numeric, graphics, performance, or version-specific behavior.
- Linear interpolation introduces a small deterministic amplitude loss; this
  lesson does not compare higher-order RCMC interpolators.
- The single known reference-path correction aligns one reference target. The
  path-following image demonstrates pixel dependence but is still a small,
  idealized point-target scene.
- Exact geometry, stationary target, isotropic response, ideal range-
  compressed sinc shape, additive complex noise, and no propagation/clutter or
  calibration error bound the scientific claim.
- Educational effectiveness still requires a human learner observation and
  teach-back; repository automation cannot prove understanding.

## Rollback

Revert only the P78 module artifacts, batch-local test/evidence, public P78
catalog additions, and restore only P78's manifest status to `scaffolded`.
Preserve P77, future module entries, learner progress, operator-managed contract
activation changes, and every forbidden path. The experiment writes no data,
so there is no external backup/restore or migration to reverse. At runtime,
recovery uses the unchanged complex input rather than inversely shifting an
already interpolated product.

## Unperformed validation

- MATLAB or compatible runtime execution, MATLAB version/toolbox inventory,
  rendered-figure inspection, runtime assertions, runtime performance, memory
  measurement, timeout, cancellation, and interactive learner teach-back;
- target Quality CI or control-plane CI on a remote runner;
- hardware, HIL, bench capture, field, real-time/RT1/RT2, operational radar,
  environmental, calibration, multi-target/wide-scene, clutter, or motion-
  error validation;
- Unreal, signing, release packaging, staging, deployment, production,
  monitoring, or rollback drill.
