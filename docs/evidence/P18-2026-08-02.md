# P18 Retained Evidence — 2026-08-02

## Claim boundary and inspected state

This record supports static repository contracts and independent Python
standard-library numerical models for P18, **Contrast Real and Complex
Sampling**. It does **not** claim MATLAB or Octave execution, MATLAB parser
compatibility, exact seed-1018 runtime metrics, rendered figures, learner
effectiveness, hardware/HIL, bench, field, RT1/RT2, real-time or operational
radar, Unreal, signing, staging, production, release, deployment, or physical
validation.

Implementation began on branch `agent/p18` at
`558112dbd630833def29848230c870dd3af557d3`, matching `main` and `origin/main`
at inspection time. One worktree was present. No repository `CODEOWNERS`,
competing worktree, target current-state file, ADR, safety record, or
compatibility record was found. The only initial working-tree change was the
operator/control-plane activation of P18 in `contracts/active-batch.yaml`; that
forbidden path was inspected and preserved outside this implementation claim.

The Quality workflow is a ten-minute Ubuntu/Python 3.12 job that runs
`./scripts/agent-verify.sh` for pull requests and pushes to `main`. Baseline run
`30760570577` completed successfully for exact head `558112d` at
`2026-08-02T18:12:09Z`. No remote CI ran for this uncommitted worktree. Before
editing, the exact contract and CI command set passed locally: curriculum
validation reported 84 modules and 17 implemented, and the existing suite ran
307 tests in 22.097 seconds with `OK`. Neither `matlab` nor `octave` was present
on `PATH`.

The repository instructions and module-builder skill; managed, DSP/radar,
active-batch, verification, architecture, implementation, tutor, and session
contracts; build and Quality workflow; shared curriculum and learner CLI tests;
P18 scaffold and canonical entry; P17 dependency implementation/test/evidence;
and canonical P18 batch, product, PRD, acceptance, M2 milestone, handoff,
architecture, assumptions, decisions, risks, roadmap, traceability, repository
ownership, automation, intake, and business plan were inspected. A bounded
read-only agent audited P16/P17 conventions and P18 compatibility hazards; the
principal agent retained all edit and integration ownership.

## Acceptance mapping

| Acceptance item | Status | Evidence |
| --- | --- | --- |
| Preserve guiding question | Pass (static) | The exact question remains in `curriculum/modules.json` and all five P18 artifacts. |
| Deterministic baseline, visible parameters, labels/metrics, sweeps, broken case | Pass (static/independent model) | `experiment.m` exposes private seed 1018, 2048 samples/s, 4096 samples, paired `+/-160 Hz` tones, explicit 600 Hz real/complex mixers and FIR, two three-case one-variable sweeps, six labeled figures, and Q-discard failure/recovery. |
| Concept-first DSP/radar model and limiting cases | Pass (static) | `lesson.md` explains I/Q rotation, conjugate real projection, signed side-of-LO, amplitude convention, DC/Nyquist/alias limits, radar use, and mistakes without syntax-first teaching. |
| Baseline, one-variable changes, failure interpretation, connection | Pass (static) | `walkthrough.md` sequences time/IQ, signed spectra, complex/real downconversion, offset and sample-rate sweeps, Q discard, recovery, and P11/P12/P16/P17 connections. |
| Interpretation and prediction checks | Pass (static) | `checks.md` covers conjugacy, phase direction, side-of-LO, aliasing, broken input representation, malformed inputs, resources, cancellation, recovery, rollback, and teach-back. |
| Dependencies declared; essential behavior visible | Pass (static) | P17 is the immediate prerequisite. The script shows exponentials, sample multiplication, phase-increment estimation, every sinc/window FIR coefficient, convolution, group-delay removal, and amplitude convention; no opaque DSP receiver/toolbox operation is called. |
| Manifest implemented only after artifacts/checks pass | Pass (static) | All five nonempty artifacts exist, P17 remains implemented, permanent-fact P18 tests are present, the shared frontier stays manifest-derived, and learner lifecycle coverage advances through P18. |
| Static and MATLAB runtime evidence separated | Pass | Independent Python math and source contracts are labeled static. No MATLAB/Octave runtime or rendered-output claim is made. |

## Figure and metric inventory

The source defines six figure groups; they were source-reviewed, not rendered:

1. **Baseline time and I/Q direction** — real/Q amplitude in V versus time in
   ms and Q in V versus I in V, with signed phase-step frequency estimates in
   Hz and real-projection RMSE in V.
2. **Signed centered spectra** — signed frequency in Hz and magnitude in dB re
   1 V for the positive complex tone, negative complex tone, and real
   projection, including peak frequencies and complex-coefficient conjugate-
   symmetry error.
3. **Real versus complex downconversion** — upper/lower complex baseband Q in V
   versus I in V, and paired real baseband amplitude in V versus time in ms;
   metrics report signed complex beats in Hz and real-output RMSE in V.
4. **Frequency-offset sweep** — 40/160/400 Hz paired I/Q trajectories, signed
   estimates in Hz, and real-projection RMSE in V.
5. **Sample-rate sweep** — 2048/512/256 samples/s signed spectra with expected
   and estimated `+160/-160`, `+160/-160`, and `-96/+96 Hz` aliases.
6. **Broken Q discard and recovery** — coincident I-only paths with zero sign
   estimates and recovered paired I/Q trajectories at `+160/-160 Hz`.

The retained `results` structure includes seed/rate/count/time, tone controls,
paired complex and real signals, signed spectra, phase-step frequencies,
projection/symmetry metrics, LO and FIR, complex and real downconversion
outputs/metrics, both sweep controls/results, and broken/recovered estimates.
The experiment writes no output files.

## Positive, negative, compatibility, and resource evidence

`tests/test_p18_module.py` uses only the Python standard library and does not
call MATLAB or Octave. It independently checks paired complex phase increments,
identical real projections, complex and real DFT amplitude/sign conventions,
complex-coefficient conjugate symmetry (including a phase-broken spectrum whose
mirrored magnitudes alone would falsely pass), upper/lower complex-LO
translation, the finite 129-tap real-mixer path, three-case offset behavior,
signed sample-rate aliasing, and Q-discard recovery.

Negative fixtures cover missing/empty artifacts, duplicate/non-list/wrong
identity, unknown controls, logical/complex/nonfinite controls, wrong/odd
records, invalid amplitude/noise/LO/filter/guard, changed sweeps or duration,
even FIR length, and changed resource ceilings. Static source contracts require
all validation before random, signal, FFT, FIR, cleanup, or figure work.

Timeout/cancellation is a bounded-source contract: one 4096-sample baseline,
one 129-tap FIR, two three-case loops, six figure groups, and no prompt, wait,
unbounded loop, timer, parallel/background, file/network/audio, system, or
opaque toolbox operation. Cancellation is not transactional: after cleanup has
begun it can leave partial P18 figures and empty/incomplete `results`; a full
rerun is the recovery. Recovery, rollback, private-RNG isolation,
P18-tagged figure cleanup, stale-result replacement, base-MATLAB compatibility,
future-batch-compatible permanent assertions, and runtime-overclaim regression
are covered.

The generic `product-data` migration, scoring, backup/restore, and service-smoke
gates are not applicable: P18 adds no persisted schema, database, scoring state,
migration, external dataset, service, credential, or license dependency.
Canonical identity/status transition, learner-state compatibility,
deterministic rerun, rollback/recovery, isolation, and bounded resources are the
applicable recoverability gates.

## Changed and preserved invariants

Changed:

- P18 now has the complete experiment, lesson, walkthrough, checks, and module
  README; its canonical manifest status is `implemented`.
- Allowed public catalogs and learner lifecycle tests expose P18 after P17.
- The manifest-derived implemented prefix advances through P18.

Preserved:

- All 84 IDs, order, folders, phases, titles, guiding questions, and batch
  identities remain canonical; implemented modules form a contiguous prefix.
- P17 remains implemented and is P18's immediate prerequisite. P18 tests assert
  permanent P17/P18 facts without requiring any later module status or naming
  P18 permanently current; shared tests derive the moving frontier.
- Learner progress stays ignored under `.learning/`; private random draws do
  not change the global stream; cleanup targets only P18 output; the experiment
  has no persisted or background side effects.
- The pre-existing forbidden active-batch activation and every other forbidden
  path remain outside the implementation claim.

## Recovery and rollback

For an invalid control, correct it and rerun; validation preserves the prior
valid output. For Q-discard failure, restore the measured Q channel and verify
signed phase progression or a centered signed FFT. Ctrl+C stops the bounded
foreground script without external or persisted side effects, but may leave
partial P18 workspace figures/results; rerunning from the top cleans and
reconstructs that module-local output.

Rollback removes P18 module artifacts, P18/shared allowed tests, P18 evidence,
and allowed catalog edits, then restores only P18 manifest status to
`scaffolded`. It preserves P17, all later canonical identities, `.learning/`
state, managed contracts, and operator activation state.

## Exact commands and results

Before editing:

```text
DSP_RADAR_VERIFY_PROFILE=contract python3 scripts/validate_curriculum.py
PASS — Curriculum validation passed: 84 modules, 17 implemented.

DSP_RADAR_VERIFY_PROFILE=quick python3 -m unittest discover -s tests -v
PASS — Ran 307 tests in 22.097s; OK.
```

After implementation:

```text
python3 -m unittest discover -s tests -p 'test_p18_module.py' -v
PASS — Post-review focused rerun ran 17 tests in 0.135s; OK.

python3 -m unittest discover -s tests -p 'test_curriculum.py' -v
PASS — Ran 6 tests in 0.087s; OK.

python3 -m unittest discover -s tests -p 'test_learn_cli.py' -v
PASS — Ran 67 tests in 10.459s; OK.

DSP_RADAR_VERIFY_PROFILE=contract python3 scripts/validate_curriculum.py
PASS — Curriculum validation passed: 84 modules, 18 implemented.

DSP_RADAR_VERIFY_PROFILE=quick python3 -m unittest discover -s tests -v
PASS — Post-review run completed 328 tests in 22.984s; OK.

DSP_RADAR_VERIFY_PROFILE=full ./scripts/agent-verify.sh
PASS — Post-review run reported 84 modules, 18 implemented, and 328 tests in
22.890s; OK. Ignored local log:
`docs/evidence/local/verify-20260802-114048.log`.

git diff --check
PASS — no whitespace errors.
```

## Independent audit and repair

The independent system-risk review found one material metric/test defect: the
source labeled a mirrored-magnitude comparison as conjugate-symmetry error.
Equal magnitudes cannot detect a phase-conjugacy violation, so the metric could
falsely report zero for a phase-broken spectrum. The script now compares the
normalized complex FFT coefficients against their conjugate-mirrored partners,
and a behavioral regression proves that a magnitude-symmetric but
phase-nonconjugate fixture fails the repaired metric. No P17 sign/amplitude,
canonical identity, forbidden-path, dependency, licensing, persisted-state, or
external-resource defect was found.

The review also confirmed the cancellation boundary: Ctrl+C is bounded and
causes no external/persisted side effect, but a partial P18 figure set and
empty/incomplete `results` can remain until a full rerun.

## Residual risks and unperformed validation

- MATLAB and Octave execution, parser compatibility, seed-1018 console metrics,
  figure rendering, and interactive learner interpretation were not performed.
- Remote Quality/control-plane CI did not run against the uncommitted worktree.
- Educational effectiveness and the receiver sign convention still require
  human review during tutoring.
- No hardware/HIL, bench, field, RT1/RT2, real-time/operational radar, Unreal,
  signing, staging, production, release, deployment, or physical validation was
  performed or claimed.
