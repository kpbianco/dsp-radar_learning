# P56 retained evidence — 2026-08-04

## Scope and claim boundary

Batch P56 implements the canonical two-dimensional range-bearing extended
Kalman-filter lesson. The retained claim covers repository structure,
curriculum identity, explicit nonlinear model/update source, deterministic
static contracts, an independent Python algebra oracle, isolated learner-CLI
behavior, bounded recovery, and reviewed instructional text.

MATLAB and Octave did not run because neither executable is installed on this
host. Static and host-language checks therefore do not prove MATLAB parsing or
execution, rendered figures, MATLAB version/toolbox compatibility, numerical
results from MATLAB, timing, memory, statistical calibration, or educational
effectiveness. No hardware/HIL, field, real-time, deployment, or production
validation was performed or is implied.

## Acceptance mapping

| Acceptance item | Implementation and evidence | Status |
|---|---|---|
| Preserve the guiding question | Exact question retained in manifest, README, experiment header, lesson, and checks; permanent identity test | Pass (static) |
| Deterministic baseline, visible controls, labeled outputs, sweeps, broken case | Seed 5601 private stream; 101 scans; five figures; bearing-noise and range-geometry sweeps; unwrapped branch-cut failure; exact wrapped recovery; source/resource and sweep-behavior tests | Pass (static/oracle) |
| Explain model and limits without syntax-first teaching | `lesson.md` explains Cartesian CV prediction, nonlinear `h`, Jacobian, metre/radian `R`, local-linearization limits, Joseph form, ellipses, NIS, and common mistakes | Pass (reviewed text) |
| Guide baseline, one-variable changes, failure, and connection | `walkthrough.md` sequences Figures 1–5, freezes non-swept inputs, interprets both sweeps and the broken case, and connects P55/P57 | Pass (reviewed text) |
| Test interpretation and prediction | `checks.md` contains observation, prediction, failure, model, resource, completion, and teach-back checks with explicit correct/incorrect interpretations | Pass (reviewed text) |
| Declare dependencies and expose essential operation | README declares P55 and P30/P18/P27 context plus P57 boundary; experiment explicitly implements `h`, `H`, wrapped innovation, `S`, gain, state, Joseph covariance, and near-origin guard | Pass (static/oracle) |
| Mark implemented only after artifacts and checks | P56 alone changes `scaffolded` to `implemented`; focused and repository checks are recorded below | Pass |
| Separate static from MATLAB runtime | This evidence and module documents state that MATLAB/Octave did not run | Pass |

## Figure and metric inventory

1. **Figure 1 — polar reports and raw Cartesian conversion:** Cartesian x/y in
   metres, range in metres, and bearing in degrees across the branch cut.
2. **Figure 2 — baseline track and uncertainty:** truth, raw conversion, EKF
   trajectory, selected nominal 95% position ellipses, and Cartesian position
   error in metres.
3. **Figure 3 — innovations and consistency:** range innovation in metres,
   wrapped bearing innovation in degrees, and dimensionless NIS against the
   two-dimensional 95% threshold.
4. **Figure 4 — two controlled sweeps:** position RMSE and tangential posterior
   standard deviation in metres plus NIS versus assumed bearing standard
   deviation in degrees; radial/tangential converted uncertainty in metres
   versus target range in metres.
5. **Figure 5 — broken case and recovery:** x/y track, bearing innovation in
   degrees, and position error in metres for wrapped and unwrapped updates.

The workspace/console metric inventory includes raw and EKF position RMSE (m),
position-ellipse and innovation coverage (fraction), mean NIS (dimensionless),
mean posterior radial/tangential standard deviations (m), bearing-sweep RMSE,
NIS, and tangential sigma, geometry-sweep radial/tangential/major sigma (m),
broken position RMSE (m), maximum broken bearing residual (deg), six filter
runs, and 600 reviewed predict/update transitions. These values were not
produced in MATLAB on this host.

## Exact commands and results

- Pre-change `DSP_RADAR_VERIFY_PROFILE=contract python3
  scripts/validate_curriculum.py` — exit 0; 84 modules and 55 implemented.
- Pre-change `python3 -m unittest tests.test_curriculum tests.test_learn_cli
  tests.test_p55_module -v` — exit 0; 113 tests passed in 5.896 seconds.
- `python3 -m unittest tests.test_p56_module -v` — exit 0; all 17 focused tests
  passed in 0.479 seconds after the final learner-interface repair.
- `DSP_RADAR_VERIFY_PROFILE=contract python3 scripts/validate_curriculum.py` —
  exit 0; `Curriculum validation passed: 84 modules, 56 implemented.`
- `DSP_RADAR_VERIFY_PROFILE=quick python3 -m unittest discover -s tests -v` —
  exit 0; all 903 repository tests passed in 34.990 seconds.
- `DSP_RADAR_VERIFY_PROFILE=full ./scripts/agent-verify.sh` — exit 0; curriculum
  validation passed and all 903 tests passed in 31.184 seconds. Ignored local
  log: `docs/evidence/local/verify-20260805-000930.log`.
- `git diff --check` and `git diff HEAD --check` — exit 0; no whitespace
  errors in the working-tree or combined changed content.

The final read-only changed-text scan — exit 0; every tracked or untracked
changed text file has LF endings, exactly one final newline, and no extra EOF
blank line or carriage return. The scope scan also passed: every implementation
change is within a P56 allowed path. The pre-existing control-plane activation
and revision-sync changes under the forbidden `contracts/active-batch.yaml` and
`contracts/repo-profile.yaml` paths were inspected, preserved, and neither
edited nor reverted by this implementation.

The exact final scope/EOF command was:

```bash
python3 - <<'PY'
from fnmatch import fnmatch
from pathlib import Path
import subprocess
root = Path.cwd()
paths = [p.decode() for p in subprocess.run(
    ['git', 'diff', 'HEAD', '--name-only', '-z'], check=True,
    capture_output=True).stdout.split(b'\0') if p]
allowed = (
    'modules/56-use-an-ekf-for-range-bearing-measurements/**',
    'curriculum/modules.json', 'README.md', 'START_HERE.md',
    'modules/README.md', 'bin/learn', 'docs/plans/P56-*.md',
    'docs/evidence/P56-*.md', 'tests/test_p56*.py',
    'tests/**/test_p56*.py', 'tests/p56/**', 'tests/**/p56/**',
    'tests/test_curriculum.py', 'tests/test_learn_cli.py',
)
preserved = {'contracts/active-batch.yaml', 'contracts/repo-profile.yaml'}
unexpected = [p for p in paths if p not in preserved and not any(
    fnmatch(p, rule) for rule in allowed)]
eof_errors = []
for rel in paths:
    data = (root / rel).read_bytes()
    if b'\0' not in data and (b'\r' in data or not data.endswith(b'\n')
            or data.endswith(b'\n\n')):
        eof_errors.append(rel)
if set(paths) & preserved != preserved or unexpected or eof_errors:
    raise SystemExit('scope/eof failure')
print(f'changed_paths={len(paths)}')
print(f'allowed_implementation_paths={len(paths) - len(preserved)}')
print('preserved_forbidden_paths=' + ','.join(sorted(preserved)))
print('scope=pass eof=pass')
PY
```

It exited 0 with 13 changed paths, 11 allowed implementation paths, both
preserved forbidden control-plane paths named, and `scope=pass eof=pass`.

## Changed and preserved invariants

Changed within allowed paths:

- completed the five P56 module artifacts;
- changed only P56 manifest status from `scaffolded` to `implemented`;
- advanced public catalog prose/status to include P56;
- added one P56-owned permanent-fact test file; and
- added this retained evidence file.

Preserved:

- all 84 module identities, canonical order, nine phases, guiding questions,
  and implementation-batch identifiers;
- P01–P55 implementation status and every P57–P84 manifest object;
- the P55 prerequisite and P57 association responsibility boundary;
- local-only learner progress and isolated CLI fixture state;
- the pre-existing control-plane activation and revision-sync diffs in
  forbidden `contracts/active-batch.yaml` and `contracts/repo-profile.yaml`,
  without editing or reverting them;
- all other forbidden paths and unrelated module/test files; and
- base-MATLAB implementation without toolbox, explicit inverse, file, network,
  shell, timer, worker, service, or learner-state effects.

## Independent review and repairs

Bounded review passes checked the local and control-plane product, architecture,
acceptance, milestone, assumptions, decision, risk, traceability, batch, and
claim contracts alongside EKF math, MATLAB plausibility, tests, pedagogy,
interfaces, and path scope. They found no defect in the state ordering,
nonlinear measurement, Jacobian signs/units, wrapped innovation, matrix-solved
gain, Joseph update, or resource accounting.

The reviews identified seven concrete presentation/robustness issues, all
repaired before final verification:

- Figure 2 now passes explicit truth/report/EKF/ellipse/radar handles to its
  legend and hides extra ellipse handles, preventing a covariance ellipse from
  being mislabeled as the radar.
- Figure 4 now places tangential sigma in metres and dimensionless mean NIS in
  separate panels instead of sharing a visually incomparable y-axis.
- The range-geometry covariance is consistently described as a
  Jacobian-mapped local first-order result, not the exact nonlinear converted
  distribution.
- Filter inputs now reject unsupported numeric classes and reviewed magnitude
  overflow; each update checks finite state, positive-definite innovation
  covariance, positive-semidefinite posterior covariance, and finite
  nonnegative NIS.
- The bearing-noise sweep now has an independent behavioral test proving that,
  on the same deterministic reports, understated `0.2 deg` noise yields much
  larger mean NIS than the reviewed `0.8 deg` case, which in turn exceeds the
  deliberately loose `3.2 deg` case. Previously, only source markers and the
  tangential covariance direction were tested.
- Resource reporting now counts 600 actual predict/update transitions across
  six runs, excluding each run's initialization slot rather than calling all
  606 scan slots filter steps.
- The geometry walkthrough now distinguishes tangential sigma from the mapped
  major axis: the 500 m case is range-dominated at 18 m before bearing-driven
  cross-range error dominates at longer range.

The final independent system-risk review identified one material public
learner-interface coverage gap: explicit `learn start 56` was tested, but the
primary no-argument progression from a completed P55 was not. The added
isolated behavioral test proves that default start selects P56 while preserving
all P01–P55 completion IDs and the existing P55 teach-back note. Post-repair
review found no remaining material correctness, concurrency, lifetime,
resource, migration, compatibility, security, licensing, recovery, claim, or
missing-test finding within P56 scope.

## Residual risks

- No MATLAB-compatible runtime was available, so script parsing, execution,
  assertions, figures, and actual reviewed metrics remain unverified.
- One seeded synthetic track cannot establish confidence calibration,
  robustness to poor initialization, or performance across dynamics/noise.
- Static source bindings plus an independent Python oracle reduce but cannot
  eliminate MATLAB-specific semantic risk.
- The local-linearization guard does not make an EKF appropriate for near-origin
  or broadly multimodal uncertainty.
- Manual lesson review does not prove learner comprehension or teaching quality.

## Rollback and recovery

Repository rollback is bounded to removing P56 module additions, P56-owned
test/evidence additions, P56 public-catalog additions, and restoring only P56
manifest status to `scaffolded`. The isolated fixture test verifies that P55 and
the complete P57 identity object are unchanged without assuming future P57
status.

Interactive cancellation recovery is deterministic: cancel with Ctrl+C, then
rerun from the top. The script validates controls before random/history work,
closes only P56-tagged figures, reconstructs its private seeded record, and
reinitializes every filter run. It persists no experiment state. Malformed
measurements, invalid covariance, near-origin geometry, and invalid resource
controls stop with named errors rather than produce partial persisted output.

## Unperformed validation

- MATLAB or Octave parsing/execution and named version/toolbox inventory;
- rendered-figure inspection and captured numerical MATLAB metrics;
- MATLAB cancellation, timeout, memory, or performance measurement;
- Monte Carlo calibration or comparison with real radar recordings;
- manual learner teach-back and educational-effectiveness study;
- Control plane and target hosted CI, because commit/push/PR actions were
  explicitly forbidden; and
- hardware/HIL, field, RT1/RT2, Unreal, signing, release, deployment, staging,
  or production validation.
