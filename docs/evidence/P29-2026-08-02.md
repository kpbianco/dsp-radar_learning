# P29 Retained Evidence — 2026-08-02

## Claim boundary

This record supports P29 repository structure, deterministic static contracts,
an independent Python radar-equation oracle, curriculum identity, learner-CLI
compatibility, bounded resources, and documented failure/recovery behavior.
MATLAB did not run, and Octave did not run. It does **not** claim MATLAB syntax
execution, MATLAB-rendered figures, MATLAB random-sample identity, hardware/HIL,
bench, field, RT1/RT2, real-time, operational radar, Unreal, signing, staging,
production, release, deployment, or human-learning validation.

- Validation class: static repository validation plus an independent
  standard-library Python analytical/simulated oracle.
- MATLAB runtime status: **NOT RUN** — `matlab` was absent from `PATH`.
- Compatible runtime status: **NOT RUN** — `octave` and `octave-cli` were absent
  from `PATH`.
- MATLAB version: unavailable.
- Toolboxes: none required; none invoked or validated.

Repository Quality CI uses Python 3.12 and `./scripts/agent-verify.sh`; it has no
licensed MATLAB step. Read-only GitHub inspection found no pull request and no
Actions run for `agent/p29`. No commit, push, pull request, merge, release,
deployment, or settings change was performed.

## Governing inputs and repository state

- Branch: `agent/p29` at starting commit `8bd8f79`, the merged P28 mainline;
  local `main`, `origin/main`, and `HEAD` pointed to that commit at inspection.
- Dependency: P28 was `implemented` before P29 work began. P29 was the next
  contiguous manifest lifecycle transition.
- Worktrees/concurrency: one registered worktree, no Git index lock, no second
  P29 worktree, and no competing MATLAB/Octave or P29 verification process were
  found. The principal agent retained edit/integration ownership. Three bounded
  helpers performed read-only module, validation, and canonical-control
  inspection; two bounded reviewers independently audited physics, MATLAB
  compatibility, pedagogy, tests, durability, and evidence.
- The session began with only `contracts/active-batch.yaml` modified, switching
  control activation from P28 to P29. `contracts/**` is forbidden implementation
  scope, so that operator/control-plane-owned diff was inspected and preserved
  without edit, revert, staging, or attribution as P29 implementation work.
- The active and canonical P29 contracts agree on behavior. The repository copy
  additionally records control-plane revision
  `aacf91301bff4dc4b52b52caf9d1cf1397ed693b`.
- Canonical `baseline_commit` still names the historical harness pointer
  `10069b5d...`, while target `HEAD` is the merged P28 commit. This is a
  control-plane metadata reconciliation concern for a future publication flow,
  not an authorized P29 repository edit.
- No separate P29 ADR, safety record, compatibility record, `CODEOWNERS`, or
  repository current-state file exists beyond the inspected AGENTS, contracts,
  canonical product/handoff/roadmap/risk/decision/traceability artifacts,
  repository architecture/implementation/tutor documents, history, build/CI/
  verification files, shared tests, P28 dependency, and P29 implementation area.
- No stop condition occurred: P29 needs no external data or toolbox, the work
  stayed within allowed implementation paths, and the expected result agrees
  with the free-space monostatic model.
- P29 adds no persistent schema, canonical dataset, scoring rule, migration,
  backup, service, package, license, credential, external transaction, or
  security boundary. The applicable product-data gates are curriculum identity,
  module completeness, deterministic checks, tutor compatibility, lifecycle
  integrity, rollback, resource bounds, and retained evidence.

## Acceptance mapping

| Acceptance item | Implemented evidence | Validation class | Status |
| --- | --- | --- | --- |
| Preserve the guiding question | Exact canonical question appears in all five P29 artifacts; immutable manifest identity is unchanged except the approved lifecycle status | Static | PASS |
| Deterministic baseline, visible controls, plots/metrics, sweeps, broken case | Private seed 2901; explicit monostatic equation; `kT0BF` threshold; RCS, fixed-gain frequency, and transmit-power sweeps; six explicit one-variable budget changes; anchored `R^-2` failure; independent `R^-4`/seed recovery | Static plus Python oracle | PASS |
| Explain model and limiting cases without syntax-first teaching | Lesson covers two spreading trips, units/dB, thermal noise, fixed-gain versus fixed-aperture frequency invariants, assumptions, limiting cases, and mistakes | Static/manual content review | PASS |
| Guide baseline, one-variable changes, failure interpretation, and connection | Walkthrough moves through baseline, RCS/frequency/power changes, sensitivity audit, slope-based failure diagnosis, recovery, range-doubling power/gain prediction, cancellation, isolation, and rollback | Static/manual content review | PASS |
| Test interpretation and prediction | Checks cover observations, range/power/gain/RCS/loss/wavelength predictions, assumptions, failure/recovery, isolation/resources, completion, and teach-back | Static/manual content review | PASS |
| Declare dependencies and avoid black boxes | P27 and P28 are declared; range equation, conversions, noise factor, threshold, margins, parameter cases, and recovery are explicit base-MATLAB arithmetic | Static | PASS |
| Mark implemented only after files and checks pass | Five nonempty artifacts, exact P29/P28 identity, public catalogs, contiguous frontier, malformed/mutation/resource tests, CLI timeout/isolation, and retained evidence participate in the gates | Static | PASS |
| Separate static validation from MATLAB runtime | This claim boundary and unperformed-validation inventory state that MATLAB/Octave did not run | Evidence policy | PASS |

## Figure and metric inventory

The source defines four P29-tagged figure groups. These figures were inspected
statically; they were not rendered in MATLAB.

1. **Baseline range loss:** received echo power in dBm versus target range in km
   on linear and logarithmic range axes. The 10-to-100 km interval is `-40 dB`.
2. **RCS and frequency sweeps:** received echo power in dBm versus range in km
   for RCS `[0.1, 1, 10] m^2`, and for `[3, 10, 30] GHz` with transmit and
   receive gains held fixed.
3. **Noise threshold and power margin:** echo/noise/threshold power in dBm versus
   range in km, zero-margin range in km, and detection margin in dB versus range
   for `[25, 100, 400] kW` transmit power.
4. **Sensitivity, failure, and recovery:** received-power change in dB at 40 km
   for explicit Pt/Gt/Gr/wavelength/loss/RCS cases; correct, broken, and recovered
   received power in dBm versus logarithmic range in km.

Retained `results` metrics include controls and resource ceilings; wavelength
in m; received and noise power in W, dBW, and dBm; threshold in dBm; margin in
dB; maximum range in km; finite seeded noise estimate in dBm; RCS/frequency/
power sweep curves; explicit one-variable inputs, power ratios, and dB changes;
range-doubling power ratio, `12.0412 dB` penalty, `16x` power/single-gain
recovery, `4x` (`+6.0206 dB`) recovery in each reciprocal antenna gain, and
three recovered-power ratios; broken/correct dB-per-decade changes; and exact
recovery status.

The fixed ceilings are 239 range points, three RCS cases, three frequencies,
three transmit-power cases, 4,096 finite-noise samples, four figure groups, and
40,000 conservatively counted numeric values. The declared estimate is 38,448.
All editable scalar controls have numeric/nonlogical/scalar/real/finite and
sign guards before random generation, array allocation, figure cleanup, or
plotting. Loops are finite; no worker, timer, file, network, device, learner-
state write, or external transaction exists.

## Independent oracle results

The standard-library Python oracle parses the canonical scalar/vector defaults
from `experiment.m` and independently evaluates the range and noise equations.
It does not execute or translate MATLAB. Mutation tests reject drift in parsed
controls, overwrites, the full baseline numerator, the range exponent, kT0BF
terms, dB-to-linear gain/loss/noise conversions, km/GHz/kW conversions, and
explicit one-variable cases.

- Wavelength: `0.0299792458 m`.
- Received power at 40 km: `4.44398e-14 W`, or `-103.522282 dBm`.
- Analytic `kT0BF` noise floor: `-109.975187 dBm`.
- Required-SNR threshold: `-96.975187 dBm`.
- Detection margin at 40 km: `-6.547094 dB`; recovery there requires
  `4.515537x` transmit power with other terms fixed.
- Baseline zero-margin range: `27.439907 km`.
- Zero-margin ranges for 25/100/400 kW: `19.402944`, `27.439907`, and
  `38.805888 km`; the fourfold-power range ratio is `4^(1/4)`.
- Doubling range divides power by 16, costs `12.041200 dB`, and requires `16x`
  transmit power or `+12.041200 dB` in `Gt*Gr`.
- The independent behavioral oracle restores the 40 km baseline power at 80 km
  through each equivalent path: `16x` transmit power, `16x` one antenna-gain
  term, or `4x` (`+6.020600 dB`) in both reciprocal gain terms.
- Correct and broken slopes are `-40` and `-20 dB/decade`; the anchored curves
  agree at 40 km but differ elsewhere.
- The Python-seeded finite-noise check produced `-109.944511 dBm`, within 10%
  in linear power of the analytic reference, and exactly repeats in Python.

Python's `random.Random.gauss` is not MATLAB's `RandStream`/`randn`; this only
supports independent repeatability and noise-power plausibility. Exact MATLAB
samples, private-stream behavior, assertions, plots, and printed values remain
unperformed runtime validation.

## Focused negative, malformed, timeout, cancellation, recovery, isolation, compatibility, and resource coverage

`tests/test_p29_module.py` uses only Python's standard library and checks:

- five artifacts, exact P29 identity, P28 prerequisite, catalogs, and isolated
  `start 29` learner entry with a ten-second subprocess timeout;
- missing/empty artifacts, non-list/duplicate/wrong manifest identity, unknown
  controls, nonnumeric/logical/complex/nonfinite/nonpositive controls, malformed
  vectors, and direct resource-overflow fixtures;
- parsed source defaults and mutation rejection for critical conversions,
  equations, overwrites, exponent, and parameter-case wiring;
- independent `R^-4`, `-40 dB/decade`, factor-16 range-doubling cost, `kT0BF`
  threshold/margin crossing, RCS/frequency trends, fourth-root power/range
  scaling, transmit-power/single-gain/reciprocal-gain recovery at doubled range,
  explicit sensitivity ratios, broken `R^-2`, and recovery behavior;
- validation ordering, fixed array/figure/storage bounds, purposeful axes/units/
  metrics, private-stream bindings, finite loops, and absence of worker/timer,
  toolbox black boxes, global RNG/figure cleanup, file/network/shell I/O;
- static/protocol guidance for `Ctrl+C`, clean rerun, caller-RNG/figure/learner-
  state isolation, base-MATLAB compatibility, local rollback, and honest
  runtime limits.

Cancellation and rollback are documented protocol/static evidence, not an
executed MATLAB cancellation or repository rollback. CLI timeout and learner-
state isolation are executed Python subprocess tests. The P29 test asserts only
permanent P29/P28 facts; shared curriculum tests derive the moving frontier and
simulate the next canonical transition, so later implemented modules do not
invalidate P29 history.

## Exact commands and results

### Repository, ownership, concurrency, runtime, and remote CI inspection

```text
git status --short --branch; git log -8 --oneline --decorate;
git branch --show-current; git worktree list --porcelain
PASS — agent/p29 at 8bd8f79; one worktree; only the pre-existing control-plane
activation was dirty before implementation.

find/rg and read AGENTS, project skill, contracts, canonical control artifacts,
handoff/current-state, risk/decision/architecture/compatibility documents,
history, ownership, implementation area, build/CI/verification files, and tests
PASS — P29 scope and dependency resolved; no additional stop condition or
ownership record found.

command -v matlab; command -v octave; command -v octave-cli
RESULT — all absent; MATLAB/Octave did not run.

gh pr list --repo kpbianco/dsp-radar_learning --head agent/p29 ...;
gh run list --repo kpbianco/dsp-radar_learning --branch agent/p29 ...
PASS after approved read-only network retry — both returned `[]`.
```

### Focused implementation iterations

```text
python3 -m py_compile tests/test_p29_module.py
PASS — focused test source compiled without error.

python3 -m unittest discover -s tests -p 'test_p29*.py' -v
EXPECTED ITERATION RESULT — before this evidence existed, 17 tests were
discovered; 16 passed and only the deliberately deferred evidence check failed.

DSP_RADAR_VERIFY_PROFILE=contract python3 scripts/validate_curriculum.py
PASS — Curriculum validation passed: 84 modules, 29 implemented.

python3 -m unittest tests.test_curriculum tests.test_learn_cli -v
PASS — Ran 97 tests in 14.819s; OK.

git diff --check
PASS — no whitespace errors.

LC_ALL=C rg -n -U '\\r' <P29 changed paths>
PASS — no carriage-return bytes found.
```

### Final local verification

```text
python3 -m unittest discover -s tests -p 'test_p29*.py' -v
PASS — Ran 18 tests in 0.283s; OK.

DSP_RADAR_VERIFY_PROFILE=contract python3 scripts/validate_curriculum.py
PASS — Curriculum validation passed: 84 modules, 29 implemented.

DSP_RADAR_VERIFY_PROFILE=quick python3 -m unittest discover -s tests -v
PASS — Ran 537 tests; OK. A supplemental non-verbose run also confirmed Ran 537 tests; OK.

DSP_RADAR_VERIFY_PROFILE=full ./scripts/agent-verify.sh
EXPECTED REVIEW ITERATION RESULT — Curriculum validation passed and 537 tests
ran; 536 passed while the retained-evidence test rejected a line break inside
its exact `confirmed Ran 537 tests` marker. The deterministic gate was preserved
and the evidence wording was repaired before rerun.

DSP_RADAR_VERIFY_PROFILE=full ./scripts/agent-verify.sh
PASS — Curriculum validation passed: 84 modules, 29 implemented; Ran 537 tests
in 31.153s; OK. Retained wrapper log:
docs/evidence/local/verify-20260802-234802.log (ignored local evidence).
```

## Independent review and repair disposition

Read-only reviews agreed that the monostatic equation, fourth-power scaling,
dB conversions, fixed-gain frequency interpretation, margin model, recovery,
and MATLAB matrix operations are structurally sound. Review findings repaired
before final validation were:

- renamed 290 K from ambiguous system temperature to input reference/source
  temperature so `kT0BF` does not imply noise-factor double counting;
- renamed complex samples to square-root-watt units and documented mean-squared
  magnitude as power;
- transposed stored sweep matrices explicitly for unambiguous MATLAB plotting;
- added per-control scalar/numeric/nonlogical/real/finite/sign guards;
- replaced hard-coded sensitivity ratios with six actual one-variable radar-
  equation recalculations;
- bound the Python oracle to parsed MATLAB defaults and mutation-tested critical
  source equations/conversions instead of relying on an independent hard-coded
  model alone;
- separated editable-control validity from immutable defaults and exercised
  actual resource-overflow branches;
- quantified gain-product recovery as +12.04 dB total, or +6.02 dB in both
  reciprocal transmit/receive gain terms;
- bound the full baseline radar-equation numerator to the static source contract
  after demonstrating that removal of receive gain previously escaped the
  validator;
- added executable doubled-range recovery cases for `16x` transmit power,
  `16x` one gain term, and `4x` in both reciprocal gain terms, plus an
  independent behavioral regression test covering all three paths;
- clarified that the 6 dB baseline loss cannot alone supply a 12.04 dB recovery
  because the model constrains the linear loss factor to remain at least one.

No remaining material physics, pedagogy, scope, test-durability, resource,
rollback, compatibility, security, licensing, or evidence defect was accepted.

## Changed and preserved invariants

Changed within P29 ownership:

- completed the five artifacts under
  `modules/29-build-a-radar-power-budget-experiment/`;
- changed only P29 manifest status from `scaffolded` to `implemented`;
- advanced public README, start guide, and module index P29 descriptions/status;
- added `tests/test_p29_module.py` and this P29 evidence record.

Preserved:

- all 84 IDs, numbers, titles, phases, folders, guiding questions, batch IDs,
  and canonical order;
- P01-P28 artifacts/status and every later module identity/content/status;
- contiguous manifest-derived implemented prefix and future-transition policy;
- ignored/local `.learning/` state, caller global random state, unrelated
  figures, external state, and every forbidden path;
- explicit distinction among watts, dBW, dBm, linear ratios, dB changes,
  deterministic margin, and statistical probability of detection;
- static/Python-oracle evidence versus MATLAB/runtime/physical claims.

## Rollback and recovery

Repository rollback removes only P29-owned module artifacts, its focused test
and evidence, approved P29 catalog text/status changes, and restores only P29's
manifest lifecycle status to `scaffolded`. It must not revert the operator-owned
`contracts/active-batch.yaml` activation, P01-P28, P30+, `.learning/`, or any
forbidden/control-plane path.

Experiment recovery independently reconstructs the correct `R^-4` equation and
fresh private stream. It does not alias the broken curve or original noise
vectors. A clean rerun after `Ctrl+C` repeats validated defaults; no file,
service, device, or external transaction requires compensation.

## Residual risks and unperformed validation

- MATLAB/Octave syntax execution, assertions, exact `RandStream` samples,
  console metrics, figures, axes rendering, legends, and interactive edits were
  not run because no compatible runtime is installed.
- Static review and a Python oracle cannot prove MATLAB numerical fidelity or
  educational effectiveness. A future named MATLAB run should retain version,
  command, stdout/assertions, toolbox inventory, and figure artifacts.
- The range equation remains an intentionally limited free-space far-field,
  matched-polarization, boresight, constant-RCS, lumped-loss power model. It does
  not validate atmosphere, multipath, clutter, fluctuation, coherent processing,
  integration, scan loss, saturation, or detection probability.
- The finite Python noise sequence is not a MATLAB RNG identity oracle.
- Control-plane `baseline_commit` reconciliation remains outside this batch.
- GitHub Quality/control-plane CI did not run because no P29 PR or branch run
  exists. Local verification is the only CI-equivalent evidence here.
- No hardware/HIL, bench, field, RT1/RT2, real-time, operational radar, Unreal,
  signing, staging, production, release, deployment, or learner teach-back was
  performed.
