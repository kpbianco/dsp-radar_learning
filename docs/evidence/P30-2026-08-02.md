# P30 Retained Evidence — 2026-08-02

## Validation class and claim boundary

- **Batch:** P30 — Measure Range from Echo Delay
- **Validation class:** static repository validation plus an independent,
  deterministic Python numerical oracle for the reviewed synthetic model.
- **MATLAB runtime status:** MATLAB did not run. `command -v matlab` returned
  exit 1; no MATLAB executable is installed in this environment.
- **Compatible runtime status:** Octave did not run. `command -v octave`
  returned exit 1; no Octave executable is installed. Octave compatibility is
  not claimed because the script uses MATLAB `RandStream`, `xline`, and
  `yline` behavior.
- **Toolboxes:** none required by the source; none executed or inspected at
  runtime.
- This evidence does **not** claim MATLAB plot rendering, MATLAB numerical
  execution, hardware/HIL, RT1/RT2, field, Unreal, signing, staging,
  deployment, release, or production validation.

Static validation and the independent oracle establish repository contracts
and deterministic model facts only. They do not establish educational
effectiveness, RF-channel fidelity, timing calibration, probability of
detection, or operational radar performance.

## Independent system-risk review — 2026-08-03

- **MEDIUM, repaired:** the fractional-delay sweep validated only that its
  integer component was nonnegative. A zero-lag case or a delay beyond the
  record could therefore reach three-point interpolation without two peak
  neighbors, causing an indexing failure instead of a preflight rejection.
- Pre-repair probe:
  `python3 -c "from tests.test_p30_module import validate_controls; print(validate_controls(fractional_delay_integer_samples=400)['fractional_delay_integer_samples'])"`
  exited 0 and printed `400`, confirming that the invalid case was accepted.
- The repair checks that every reviewed rate represents at least one pulse
  sample and that all fractional-sweep cases retain a complete pulse plus a
  two-sided correlation-peak neighborhood inside the capture before signal
  allocation, RNG creation, cleanup, or plotting.
- The added behavioral test rejects zero-lag, out-of-capture, and zero-sample
  pulse controls and binds the corresponding MATLAB preflight assertions.

## Acceptance mapping

| Acceptance item | Result | Evidence |
| --- | --- | --- |
| Preserve the exact guiding question | PASS | All five artifacts and the exact P30 manifest identity contain “How does round-trip delay become target range?” |
| Deterministic baseline with visible controls | PASS | Seed 3001, fixed finite controls, private `RandStream`, explicit fractional echo insertion, pre-allocation pulse/peak-neighborhood bounds, and retained `results.baseline` |
| Labeled plots and metrics | PASS | Six bounded figure groups cover pulse/echo, correlation/range, sample rate, fractional delay, two targets, and factor-of-two recovery |
| Meaningful parameter sweeps | PASS | Fixed-physical-delay rates 10/20/40 MHz, fractional delays 0/0.25/0.5/0.75 samples, and second-target separations 0.5/1.0/1.5 microseconds |
| Intentionally broken case and recovery | PASS | `R_broken = c*tau` is exactly twice `c*tau/2`; a recreated private stream exactly recovers the baseline vectors and correlation in source contracts |
| Concept-first lesson and limits | PASS | `lesson.md` connects correlation lag, seconds, two-way geometry, sample bins, interpolation, and finite-pulse peak width without syntax-first teaching |
| Guided walkthrough | PASS | Baseline observation, two required one-variable sweeps, second-target separation, broken interpretation, recovery, cancellation, and rollback are explicit |
| Interpretation and prediction checks | PASS | `checks.md` includes observation, prediction, interpretation, failure/recovery, malformed-input/resource, completion, and teach-back checks |
| Dependencies and transparent processing | PASS | P08 and P29 are declared; echo interpolation and lag-by-lag conjugate correlation are explicit; no `xcorr`, `circshift`, toolbox detector, or external I/O is used |
| Manifest/catalog transition | PASS | P30 alone changed from `scaffolded` to `implemented`; public catalogs expose P30 without freezing future module state |
| Honest evidence boundary | PASS | MATLAB and Octave absence and every unperformed validation class are stated above and below |

## Independent oracle results

The Python oracle in `tests/test_p30_module.py` independently implements
zero-extended piecewise-linear echo insertion, valid-lag rectangular-pulse
correlation, three-point peak refinement, sample/range conversion, and local
peak counting. It does not execute or parse MATLAB numeric output.

| Metric | Reviewed result |
| --- | ---: |
| True round-trip delay | 6.0175 microseconds |
| True delay on the 20 MHz grid | 120.350000 samples |
| True monostatic range | 902.000558 m |
| Integer correlation lag | 120 samples |
| Refined correlation lag | 120.269231 samples |
| 20 MHz integer range-bin spacing | 7.494811 m |
| Integer range error | -2.623184 m |
| Refined range error | -0.605350 m |
| Visible peaks at 0.5/1.0/1.5 microseconds | 1 / 1 / 2 |
| Broken one-way / recovered range ratio | 2 / 1 |

The rate oracle also confirms that 10, 20, and 40 MHz bin spacings halve at
each step and each noise-free integer estimate remains within its half-bin
bound. Fractional-delay cases remain within 0.5 sample at integer lag, and the
reviewed parabolic estimates reduce the maximum staircase error. These are
facts about this finite-pulse synthetic model, not universal estimator bounds.
The behavioral control oracle also rejects a zero-lag fractional sweep, a
fractional sweep whose pulse/peak neighborhood exceeds the capture, and a
sample rate that represents the finite pulse with zero samples.

## Figure and metric inventory

| Figure | Purpose | Axes / units | Retained metrics |
| --- | --- | --- | --- |
| Baseline transmitted pulse and echo | Show known finite pulse, continuous fractional delay before sampling, and seeded noise | time in microseconds; amplitude in V | pulse and record samples, clean/received vectors |
| Correlation lag becomes range | Expose explicit matched alignment and the `c*tau/2` mapping | monostatic range in m; correlation in V^2 samples | true, integer, and refined delay/range; errors; peak |
| Sample-rate sweep | Hold physical delay/pulse fixed while grid changes | sample rate in MHz; error and bin spacing in m | integer/refined ranges and errors; `c/(2*fs)` |
| Fractional-delay sweep | Show integer staircase and local interpolation | fractional delay and error in samples | integer/refined estimates and errors |
| Two-target separation | Show merged response versus two visible peaks | range in m; correlation in V^2 samples | separations in microseconds and m; peak counts |
| Broken/recovery comparison | Isolate the missing return-trip factor | reported range in m | broken factor 2, recovered factor 1, recovery exact-match flag |

Resource ceilings are 640 record samples, 40 pulse samples, 640 correlation
samples, four cases per sweep, six figure groups, and 50,000 conservatively
counted numeric values. The reviewed estimate is 28,800 values. There is no
unbounded loop, worker, timer, pause, external transaction, file, network, or
device operation.

## Exact commands and results

Commands are listed exactly as required by the active contract. Completion
results below are updated only from commands actually run in this worktree.

1. `python3 -m py_compile tests/test_p30_module.py`
   - PASS — exit 0.
2. `DSP_RADAR_VERIFY_PROFILE=contract python3 scripts/validate_curriculum.py`
   - PASS — exit 0: `Curriculum validation passed: 84 modules, 30 implemented.`
3. `python3 -m unittest tests.test_p30_module -v`
   - Initial bootstrap result: 13 substantive tests passed and the retained-
     evidence existence test failed because this file did not yet exist.
   - Pre-review baseline: exit 0, `Ran 14 tests in 0.182s`, `OK`.
   - PASS — post-repair focused rerun exit 0: `Ran 15 tests in 0.189s`, `OK`.
4. `DSP_RADAR_VERIFY_PROFILE=quick python3 -m unittest discover -s tests -v`
   - PASS — post-repair exit 0: `Ran 552 tests`, `OK`.
5. `DSP_RADAR_VERIFY_PROFILE=full ./scripts/agent-verify.sh`
   - PASS — exit 0. The wrapper reported
     `Curriculum validation passed: 84 modules, 30 implemented.`, then
     `Ran 552 tests in 30.429s`, `OK`.
   - Ignored local log:
     `docs/evidence/local/verify-20260803-002346.log`.
6. `git diff --check`
   - PASS — exit 0 before completion verification.
7. `command -v matlab`
   - NOT AVAILABLE — exit 1; MATLAB did not run.
8. `command -v octave`
   - NOT AVAILABLE — exit 1; Octave did not run.

## Changed and preserved invariants

Changed within P30 ownership:

- completed the five P30 module artifacts;
- changed only P30 manifest status to `implemented` while preserving its
  number, ID, title, phase, folder, guiding question, and batch identity;
- updated the allowed public catalogs for the P30 learning transition;
- added P30-only permanent tests and this P30-only retained evidence;
- added a pre-allocation guard that keeps every fractional-sweep pulse and its
  two-sided correlation-peak neighborhood inside the capture, plus a behavioral
  regression test for the previously accepted edge/out-of-capture controls.

Preserved:

- exactly 84 modules in canonical order and one contiguous implemented prefix;
- P29 and every unrelated module artifact/status;
- all future batches' ability to advance the shared frontier; P30 tests do not
  require later batches to remain scaffolded and do not call P30 permanently
  latest;
- the operator-managed dirty `contracts/active-batch.yaml` without editing it;
- every forbidden path, CI workflow, verification command, architecture/tutor
  contract, Makefile, phase catalog, and ignored learner state;
- no commit, push, PR, merge, release, deployment, or settings change.

## Rollback and recovery

Repository rollback removes P30-owned module additions, P30 tests, and this
evidence; restores the prior P30 README/catalog wording; and changes only P30's
manifest status back to `scaffolded`. It must preserve the operator-managed
active-batch contract, P29, later module work, verification infrastructure, and
all ignored `.learning/` data.

Runtime cancellation is `Ctrl+C`, followed by a clean rerun from the top.
Malformed controls stop before allocation, private RNG creation, figure
cleanup, or plotting. A clean recovery rebuilds a new private seed, received
record, and correlation and restores `c*tau/2`; partial workspace values are
not accepted as a completed result.

## Residual risks and unperformed validation

- MATLAB did not run, so MATLAB syntax execution, assertions, printed metrics,
  `RandStream` reproduction, figure creation, labels, and rendering are
  unperformed.
- Octave did not run and compatibility is unperformed/not claimed.
- No licensed toolbox inventory was queried because no MATLAB runtime exists;
  the static source requires base MATLAB only.
- The explicit fractional-delay model is piecewise linear, not a bandlimited
  propagation channel or calibrated RF timing model.
- Parabolic interpolation behavior is waveform-, SNR-, and peak-shape-specific.
- No learner session or teach-back was performed, so educational effectiveness
  and personal module completion remain manual gates.
- Required CI was not triggered locally; `.github/workflows/quality.yml` was
  inspected but protected and unchanged.
- Hardware/HIL, RT1/RT2, field capture, real-time, operational radar, Unreal,
  signing, staging, release, deployment, and production validation were not
  performed and are not claimed.
