# P81 retained evidence — 2026-08-14

## Claim boundary

P81 provides a deterministic, bounded, base-MATLAB source experiment and a
complete concept-first lesson package for synthetic stepped-frequency data.
The independent Python oracle exercises the declared complex echo,
range-compression, translation-alignment, and angle-FFT equations as simulated
numerical evidence. Repository checks and source inspection are static.

No MATLAB or compatible executable is installed. This evidence does not claim
MATLAB parsing, execution, figure rendering, or MATLAB resource/runtime
behavior. It also does not claim real scattering fidelity, physical radar/HIL,
bench, field, real-time, RT1/RT2, Unreal, operational radar, signing, release,
deployment, or production validation.

## Governance, current state, ownership, concurrency, and CI inspection

- Repository instructions, the complete module-builder skill, repository/profile
  and DSP requirements, active batch, verification contracts/scripts, named
  tests, module README, public catalogs, CI workflow, and build file were read.
- Canonical product, PRD, acceptance, architecture, assumptions, decisions,
  repository ownership, risks, roadmap, traceability, intake/handoff, DRL-M9,
  automation, and P81 batch artifacts were inspected in the control worktree.
- Branch agent/p81 began at 8804c4a, equal to origin/main, after completed P80.
  No competing worktree owns agent/p81.
- The only pre-existing change was Portfolio activation of
  contracts/active-batch.yaml from P80 to P81. It remains operator-owned and was
  preserved.
- Implementation changes stay inside P81 module/test/evidence and the explicitly
  allowed manifest/catalog files. No forbidden path was implementation-edited.
- The Quality workflow runs the repository harness with a 10-minute job bound.
  The latest main/P80 run, 31756088902, succeeded in 2m58s. P81 has no CI run
  because the branch remains uncommitted and unpushed as requested.
- Two bounded read-only subagents audited prior-module conventions and P81
  physics. The principal agent retained integration and write ownership.

## Acceptance map

| Acceptance | Implementation and retained evidence |
|---|---|
| Guiding question | Exact canonical question appears in all five artifacts, manifest, and tests. |
| Deterministic baseline | Seed 8101, private Park-Miller values, visible 10 GHz/600 MHz/65-look controls, assertions, metrics, and five tagged figures. |
| Transparent model | Explicit two-way scatterer phase, base IFFT range compression, full-frequency translation correction, and base FFT across aspect. |
| Sweep 1 | Aperture [2 4 6 8] deg changes nominal lambda/(2 Delta-theta) scale with target, carrier, bandwidth, and look count held fixed. |
| Sweep 2 | Rate [3 6 12] deg/s holds the same 6 deg/65-look aspect grid and separates CPI/Doppler hertz from angle-domain geometry. |
| Broken case | Raw translating profiles bypass compensation; independent full-image correlation falls to 0.2466836224. |
| Recovery | Reprocesses unchanged raw complex history and exactly matches the earlier aligned history/image. |
| Lesson/walkthrough/checks | Physical equations, limiting cases, one-transition guidance, 48 answered checks, and teach-back rubric. |
| Dependencies/compatibility | P18, P30, P36, P61-P63, and P75-P80 declared; base MATLAB R2016b+; no toolbox/external data. |
| Negative/bounded behavior | Tests reject boolean, nonfinite, nested, nonpositive, noninteger, unordered, mismatched, out-of-support, and over-ceiling inputs. |
| Coordinate handedness | An asymmetric positive-cross-range point localizes to positive cross-range and its expected range bin; the test also binds the MATLAB `-(lambda/2) f_theta` mapping. |
| Rollback/isolation/timeout | Temporary CLI fixtures cover P81 scaffold rollback, P80 fallback, restore, P82 future metadata, 3-second timeout, and learner-state isolation. |
| Permanent tests | P81/P80 permanent facts are asserted; only P82 identity/batch is asserted; a later implemented P82 is simulated. |
| Manifest transition | P81 becomes implemented only with complete artifacts and passing deterministic checks. |
| Honest evidence | MATLAB absence and all unperformed runtime/physical/release work are explicit. |

## Deterministic simulated-oracle results

The standard-library Python oracle does not import or execute MATLAB source. It
independently constructs the full 65 by 129 complex history for ten seeded
scatterers, performs centered DFT/IFFT operations, forms the full 129 by 65
range/cross-range image, and compares aligned, broken, and recovered paths.

| Quantity | Result |
|---|---:|
| Carrier / wavelength | 10 GHz / 0.03 m |
| Bandwidth / frequency samples | 600 MHz / 129 |
| Angle / looks / rate / CPI | 6 deg / 65 / 6 deg/s / 1 s |
| Nominal range resolution | 0.25 m |
| Nominal cross-range resolution | 0.1432394488 m |
| Aligned truth-neighborhood power capture | 0.95932265626204 |
| Aligned truth/background median power ratio | greater than 1,000,000 |
| Broken truth-neighborhood capture | less than 0.20 |
| Broken/aligned magnitude correlation | 0.24668362243508937 |
| Asymmetric point truth / localized coordinates | (range 0.75 m, cross-range +1.13 m) / (0.7441860465 m, +1.1282861196 m) |
| Raw history after failure path | element-for-element unchanged |
| Recovered history/image | element-for-element equal to aligned baseline |

The full-image sweep oracle retains aperture truth-map correlations
[0.7290789461, 0.7545098349, 0.7733181350, 0.7920998389], the 6 deg scale
0.1432394487827058 m, CPI values [2, 1, 0.5] s, and fixed-aspect rate image
correlations numerically equal to one. MATLAB source assertions additionally
require layout improvement above 0.05, rate correlations above 1 - 1e-10, and
broken/recovered correlations below 0.65/above 1 - 1e-12. Those source
assertions were not executed in MATLAB here.

## Figure and metric inventory

| Figure | Axes/metrics | Meaning |
|---|---|---|
| Range compression/alignment | Range m, aspect deg, normalized dB | Raw 2 m centroid walk versus envelope/carrier alignment. |
| Baseline ISAR image | Cross-range m, range m, truth marks, normalized dB | Recognizable rigid point layout after explicit processing. |
| Aperture sweep | Aperture deg, nominal resolution m, dimensionless correlations/capture | Angular support sharpens cross-range while sampling support changes. |
| Rate sweep | Rate deg/s, CPI s, implied PRF Hz, rotational Doppler Hz, image correlation | Same aspect grid preserves geometry while time sampling changes. |
| Broken/recovered | Cross-range/range m, normalized dB, image correlation | Wideband range walk/coupling smears while linear carrier phase shifts; same-history processing recovers. |

Display dB is peak-normalized only for visibility. Truth capture,
truth/background ratio, and reference correlation use unclipped linear/complex
values. The resolution numbers are model scales, not calibrated measurements.

## Resource, timeout, cancellation, and side-effect evidence

- Planned and checked synthesis work is
  (1 + 4 + 3) histories times 65 looks times 129 frequencies times 10 targets,
  exactly 670,800 contributions under a 900,000 ceiling.
- Frequency samples, looks, targets, sweep cases, and private values are capped
  at 257, 129, 16, 6, and 64.
- Retained MATLAB function workspace is capped at 2,500,000 eight-byte value
  equivalents. This is a live inventory, not peak memory, and did not run here.
- CLI fixtures use a 3-second subprocess timeout. Conditional MATLAB validation
  uses 300 seconds when an executable exists and skipped here.
- Finite foreground loops perform no file/network I/O, timer, worker, GPU,
  shell, checkpoint, or persistent state. Ctrl+C was documented but not forced.

## Exact commands and results

Pre-edit/current-state inspection:

~~~text
git status --short --branch
git branch --show-current
git log -8 --oneline --decorate
git worktree list --porcelain
git rev-list --left-right --count origin/main...HEAD
~~~

Exit 0: branch agent/p81, 0/0 divergence at 8804c4a, no competing branch
worktree, and only the operator-owned activation contract dirty.

~~~text
gh run list --workflow Quality --limit 5
git ls-remote --heads origin main
~~~

Exit 0: latest P80 main Quality run 31756088902 succeeded in 2m58s; remote main
resolved to 8804c4a0e4661a2193663357e6327732a8d941b1.

~~~text
command -v matlab
command -v octave
matlab -batch "disp(version)"
octave --version
~~~

Neither executable was found; shell attempts returned command-not-found. No
MATLAB-compatible runtime result is claimed.

Focused final checks:

~~~text
DSP_RADAR_VERIFY_PROFILE=contract python3 scripts/validate_curriculum.py
python3 -m unittest tests.test_p81_module -v
~~~

Exit 0: curriculum validation reported 84 modules, 81 implemented; all 14 P81
tests ran in 7.282 seconds, 13 passed, and the actual-MATLAB gate was the one
explicit skip.

~~~text
python3 -m py_compile tests/test_p81_module.py
git diff --check
~~~

Exit 0: Python source compiled and no whitespace error was reported.

Shared validation:

~~~text
DSP_RADAR_VERIFY_PROFILE=quick python3 -m unittest tests.test_curriculum tests.test_learn_cli -v
~~~

Exit 0: all 97 shared manifest/frontier and learner CLI tests passed in
5.759 seconds.

Full repository validation:

~~~text
DSP_RADAR_VERIFY_PROFILE=full ./scripts/agent-verify.sh
~~~

Exit 0: curriculum validation reported 84 modules and 81 implemented; all
1,314 discovered tests ran in 164.128 seconds, with 1,310 passing and the four
P78-P81 actual-MATLAB gates explicitly skipped because MATLAB is unavailable.
The final harness ran through 2026-08-14T00:53:26+00:00 and retained its ignored
local log at docs/evidence/local/verify-20260814-005041.log.

## Changed and preserved invariants

Changed by P81:

- completed P81 README and added experiment, lesson, walkthrough, and checks;
- changed only P81 manifest/index status from scaffolded to implemented;
- advanced manifest-derived catalog prose/count/frontier from P80 to P81;
- added permanent P81 tests, including an asymmetric handedness regression,
  and this evidence.

Preserved:

- canonical 84-module identity, order, questions, folders, phases, and batches;
- P01-P80 implemented prerequisite prefix and all P82-P84 identities/statuses;
- P81 canonical question, experiment, procedure, goal, and completion condition;
- transparent base-MATLAB processing with no toolbox/external data;
- ignored learner progress and operator-owned batch activation;
- every forbidden path;
- no commit, push, PR, merge, release, deployment, settings, hardware, or
  production action.

Tests enforce exactly one final newline and no extra EOF blank line for each
P81-changed text file.

## Residual risks

- MATLAB parsing, execution, figures, assertions, RNG isolation, repeatability,
  storage bound, and measured runtime remain unvalidated.
- The full Python oracle is not MATLAB and cannot prove identical FFT
  indexing/rendering behavior.
- The angle FFT is a small-angle approximation. Larger objects/aspects require
  migration correction or exact geometric focus.
- Isotropic points omit occlusion, scintillation, 3-D projection, clutter,
  multipath, noise, acceleration, and motion-estimation uncertainty.
- Translation is known exactly and linear. Its ordinary slow-time Doppler is
  aliased at the reviewed PRFs, so sample-by-sample unwrapped displacement is
  supplied externally; operational estimation is more ambiguous and
  failure-prone.
- Ctrl+C was not forced, peak memory was not measured, and the unpushed P81
  branch has no CI run.
- Human review is still needed for educational effectiveness and visual
  recognizability.

## Rollback

Remove only the four added module files, restore the P81 README scaffold, remove
the P81 test/evidence, restore only P81 manifest/index status to scaffolded, and
derive root catalog frontier prose from that state. Preserve P80, P82-P84,
personal learning state, operator-managed active-batch contract, verification
infrastructure, and all forbidden paths.

The broken runtime path mutates no external state. Rerunning closes stale
P81-tagged figures and rebuilds deterministic private inputs.

## Unperformed validation

- MATLAB or compatible parsing/execution, repeat comparison, rendering,
  RNG/storage assertions, and measured runtime
- forced Ctrl+C cancellation and timing
- physical radar/target capture, HIL, bench, field, real-time, RT1/RT2, Unreal,
  operational/scattering/RCS validation
- signing, packaging, release, staging, deployment, production, or settings
- P81 GitHub Actions CI on this uncommitted/unpushed branch
- commit, push, pull request, merge, release, or deployment
