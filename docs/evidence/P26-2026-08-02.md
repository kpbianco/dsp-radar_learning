# P26 Retained Evidence — 2026-08-02

## Claim boundary

This record supports P26 repository structure, deterministic static contracts,
independent standard-library Python behavioral checks, curriculum identity,
learner-harness compatibility, and bounded failure/recovery behavior. MATLAB
and Octave did not run. It does **not** claim MATLAB-rendered figures,
MATLAB-runtime numerical fidelity, hardware/HIL, bench, field, RT1/RT2,
real-time, operational radar, Unreal, signing, staging, production, release,
deployment, or human-learning validation.

`matlab`, `octave`, and `octave-cli` were absent from `PATH`. The Quality
workflow uses Python 3.12 and `./scripts/agent-verify.sh`; it has no licensed
MATLAB step. Read-only GitHub inspection returned no P26 pull request and no
P26 branch Actions run. No commit, push, PR, merge, release, deployment, or
settings change was performed.

## Governing inputs and repository state

- Branch: `agent/p26` at starting commit `7a1cbe3`, the merged P25 mainline;
  `main` and `origin/main` pointed to the same commit during inspection.
- Dependency: P25 was `implemented` before P26 work began. P26 was the next
  contiguous manifest transition.
- Worktrees/concurrency: one registered worktree; no second P26 worktree,
  MATLAB/Octave process, P26 PR, or P26 Actions run was found.
- The sole pre-existing worktree modification was
  `contracts/active-batch.yaml`, which activates P26. That file is a forbidden
  control-plane/operator path and was preserved without authorship, edit,
  revert, staging, or inclusion as P26 implementation work.
- Active and canonical P26 contracts match in goal, dependency, scope,
  acceptance, validation, risk, rollback, and evidence. The repository copy
  additionally retains managed revision metadata.
- `portfolio status dsp-radar-learning` selected P26 and reported no stop gate
  or open control/target/pointer PR. It also reported pre-existing control-plane
  pointer drift; no pointer or control-plane mutation was authorized.
- Three bounded pre-implementation read-only audits covered governance,
  numerical LMS design, and persistent validation. A bounded integrated
  read-only risk review checked the resulting diff. The principal agent kept
  sole edit/integration ownership.
- No repository P26-specific ADR, safety record, compatibility record,
  CODEOWNERS file, or current-state document exists beyond the inspected
  governance, canonical handoff, contracts, history, CI/build/verification
  files, module brief, prerequisite implementation, and control artifacts.

## Acceptance mapping

| Acceptance item | Implemented evidence | Validation class |
| --- | --- | --- |
| Preserve the guiding question | Exact canonical question appears in all five P26 artifacts; manifest identity is unchanged except approved lifecycle status | Static |
| Deterministic experiment, visible controls, plots/metrics, sweep, broken case | `experiment.m`: private seed 2601, 6,000 samples, explicit eight-tap LMS, five figure groups, midpoint path change, useful-signal coherent-gain checks, four-case step sweep, five-case reference-correlation sweep, guarded oversized-step failure, and stable reset/recovery | Static plus independent Python model |
| Explain model and limiting cases | `lesson.md`: reference/primary coupling model, LMS equations, distinct mean-coefficient and mean-square convergence limits plus a conservative study rule, path-change transient, reference-quality limit, spectra, limiting cases, misconceptions, and radar/DSP connection | Static/manual content review |
| Guide baseline, one-variable changes, failure interpretation, and connection | `walkthrough.md`: signal flow, coefficient/reacquisition transition, residual spectrum, fixed-data step sweep, fixed-primary reference sweep, bounded failure, deterministic recovery, completion connection, and operational recovery | Static/manual content review |
| Test interpretation and prediction | `checks.md`: observation, prediction, false-interpretation correction, failure/recovery, resource/isolation, completion, and teach-back checks | Static/manual content review |
| Declare dependencies and avoid black boxes | P25 and P07 are declared; FIR coupling convolution, reference vectors, prediction, subtraction, coefficient update, mismatch, power, and FFT are explicit base-MATLAB operations | Static |
| Mark implemented only after artifacts/checks pass | Five nonempty artifacts, P25 prerequisite, exact P26 identity/status, public catalogs, frontier integrity, isolated learner entry, malformed inputs, bounded resources, behavioral oracles, and evidence are checked | Static |
| Separate static from MATLAB runtime evidence | This claim boundary and unperformed inventory explicitly state that MATLAB/Octave did not run | Evidence policy |

## Experiment and figure/metric inventory

All axes and metrics identify physical units or normalized dimensionless units.

1. **Signal flow and cancellation:** primary versus desired, true versus
   estimated interference, and canceller error amplitude versus time in
   milliseconds, with the path change marked; settled clean-waveform coherent
   gain is retained before and after the change.
2. **Learned coupling coefficients:** eight learned FIR coefficients and RMS
   coefficient mismatch versus sample index, including the 0.08 mismatch and
   64-consecutive-sample reacquisition rule.
3. **Learning power:** 128-sample moving canceller-output and residual-
   interference mean-square power in dB relative to one normalized unit versus
   sample index.
4. **Residual spectra:** explicit Hann-windowed 2,048-point FFT bin power in dB
   relative to one normalized unit squared versus frequency in hertz, before
   and after the path change.
5. **Sweeps and guarded failure:** reacquisition samples and late output power
   versus dimensionless LMS step size; residual suppression in dB versus
   reference correlation; guarded broken-run error power versus sample index.

The deterministic source model uses an 8 kHz sample rate, two desired tones at
700 Hz and 1,100 Hz with amplitudes 0.25 and 0.18 normalized units, independent
receiver noise standard deviation 0.05, and a unit-mean-square Gaussian
reference. The eight-tap path changes at MATLAB sample 3001 from
`[0.80,-0.50,0.30,0.20,-0.15,0.10,0.05,-0.03]` to
`[0.35,0.70,-0.45,0.25,0.15,-0.10,0.08,0.03]` without resetting the baseline
weights. Stable study steps are `[0.0005,0.002,0.006,0.012]`; baseline is
`0.006`; the guarded broken step is `0.35`.

Retained `results` include controls; reference, desired, primary, and true
interference; both paths; baseline estimate/error/residual/weight history/path
mismatch/power/suppression/reacquisition and pre/post desired coherent gain;
both sweep inputs and metrics; broken guard/error/weight norm/stop sample;
deterministic recovery; and pre/post frequency axes and spectra. Fixed ceilings
are 6,000 samples, eight taps, four step cases, five reference cases, a
2,048-point FFT, five figure groups, and a conservative 500,000 stored-numeric-
value budget.

## Independent behavioral evidence

The independent standard-library Python oracle does not execute, parse, or
translate MATLAB and does not reproduce MATLAB's private random stream. It uses
the same physical controls with Python's seeded Gaussian sequence to check the
algorithmic behavior:

- baseline residual-interference suppression was `29.8823 dB` before and
  `29.1945 dB` after the path change;
- clean desired-waveform coherent gain was `1.005889` before and `1.003958`
  after the change; cancellation improved normalized correlation with that
  waveform from `0.196804`/`0.211912` at the primary input to
  `0.964745`/`0.964182` at the output;
- RMS tap mismatch was `0.005761` before the change, jumped to `0.542615` at
  the change, fell to `0.015416`, and satisfied the held reacquisition rule in
  385 samples;
- step sizes `[0.0005,0.002,0.006,0.012]` reacquired in
  `[not within record,1019,385,223]` samples; their late output powers were
  `[0.200495,0.050829,0.051605,0.052747]` normalized squared units, exposing
  slow learning and rising misadjustment;
- reference correlations `[1,0.75,0.5,0.25,0]` produced settled suppression
  `[29.1945,3.4403,1.1085,0.1654,-0.0884] dB`, demonstrating that a stable
  filter cannot replace an informative reference;
- the oversized step triggered the independent finite guard at sample 104;
  two stable zero-initialized runs reproduced identical errors and final taps.

These checks support the FIR/LMS equations, useful-signal preservation,
convergence transition, reacquisition, sweep mechanisms, failure bound, and
recovery. They do not prove MATLAB syntax, MATLAB-runtime results, MATLAB
random samples, plotted figures, or instructional effectiveness.

## Focused, negative, recovery, isolation, compatibility, and resource coverage

`tests/test_p26_module.py` uses Python's standard library and checks:

- five artifacts, exact identity, P25 prerequisite, public catalogs, and an
  isolated P26 learner start with a ten-second subprocess timeout;
- missing, empty, duplicate, non-list, and every wrong-identity case;
- fixed finite controls plus logical, complex, nonfinite, malformed-vector,
  wrong-shape/order, noncanonical, unknown, and oversized rejection before
  random work, allocation, convolution, FFT, cleanup, or figures;
- independent LMS baseline/path-change behavior, clean desired-waveform gain
  and correlation preservation, both one-variable sweeps, guarded instability,
  exact rerun recovery, and source markers binding each independently checked
  processing transition to the MATLAB script;
- exact LMS equations, private stream isolation, desired/reference/path model,
  distinct mean-coefficient/mean-square/study bounds, coefficient mismatch,
  spectra, labeled metrics, retained results, and five-figure/resource ceilings;
- placeholder, opaque adaptive-filter/toolbox, external-I/O, unbounded loop,
  worker, timer, and global-figure-cleanup regressions;
- Ctrl+C cancellation guidance, full rerun recovery, global RNG and learner-
  state isolation, caller-workspace boundary, base-MATLAB compatibility,
  external-transaction absence, and rollback language;
- an honest retained-evidence claim boundary.

The P26 suite asserts permanent facts about P26 and P25 only. It does not
require any later status or call P26 permanently latest. Shared curriculum and
learner tests derive the moving implementation frontier from canonical state
and simulate a valid later transition.

## Exact commands and results

### Repository, ownership, concurrency, runtime, and remote CI inspection

```text
git status --short --branch; git log -8 --oneline --decorate; git worktree list --porcelain; git branch -vv
PASS — agent/p26 at 7a1cbe3; one worktree; only operator-owned active-batch
activation was dirty before implementation; history, branch ownership, staged
state, and concurrency were inspected.

portfolio status dsp-radar-learning
PASS (read-only) — P26 selected; stop gate none; open control/target/pointer
PRs all zero; checkout and origin at 7a1cbe3; known pointer drift reported.

gh pr list --repo kpbianco/dsp-radar_learning --head agent/p26 --state all ...
gh run list --repo kpbianco/dsp-radar_learning --branch agent/p26 ...
PASS (read-only after approved network retry) — both returned `[]`.

command -v matlab; command -v octave; command -v octave-cli
RESULT — all three runtimes absent.
```

### Focused implementation iterations

```text
python3 -m py_compile tests/test_p26_module.py
PASS — P26 test source compiled without error.

DSP_RADAR_VERIFY_PROFILE=contract python3 scripts/validate_curriculum.py
PASS — Curriculum validation passed: 84 modules, 26 implemented.

python3 -m unittest discover -s tests -p 'test_p26_module.py' -v
EXPECTED ITERATION FAILURE — 17 tests ran; the sole failure was the deliberately
deferred retained-evidence existence check. The other 16 artifact, identity,
model, sweep, malformed-input, resource, isolation, recovery, and CLI checks
passed.

DSP_RADAR_VERIFY_PROFILE=quick python3 -m unittest discover -s tests
EXPECTED ITERATION FAILURE — 483 tests ran in 29.124s; the sole failure was the
deliberately deferred P26 evidence check. All other 482 repository tests
passed.

git diff --check
PASS — no whitespace errors.

LC_ALL=C rg -n -U '\r' modules/26-use-lms-to-cancel-an-interferer ...
REVIEW-DRIVEN REPAIR — the integrated review found two carriage-return bytes
corrupting inline `rho` text in `lesson.md`; the text was repaired, the scan
then returned no matches, and `git diff --check` remained clean.
```

### Final local verification

```text
python3 -m unittest discover -s tests -p 'test_p26_module.py' -v
PASS — Ran 18 tests; OK, including artifact/identity, independent LMS, desired-
signal preservation, sweep behavior, malformed controls, bounded failure/
recovery, isolation, learner entry, and retained evidence.

DSP_RADAR_VERIFY_PROFILE=contract python3 scripts/validate_curriculum.py
PASS — Curriculum validation passed: 84 modules, 26 implemented.

DSP_RADAR_VERIFY_PROFILE=quick python3 -m unittest discover -s tests
PASS — Ran 484 tests in 28.567s; OK.

DSP_RADAR_VERIFY_PROFILE=full ./scripts/agent-verify.sh
PASS — Curriculum validation passed with 84 modules and 26 implemented; Ran
484 tests; OK. The script retained its timestamped ignored local log under
`docs/evidence/local/`.

LC_ALL=C rg -n -U '\r' modules/26-use-lms-to-cancel-an-interferer tests/test_p26_module.py docs/evidence/P26-2026-08-02.md README.md START_HERE.md modules/README.md curriculum/modules.json
PASS — no carriage-return bytes found after the review-driven repair.

git diff --check
PASS — no whitespace errors.
```

## Changed and preserved invariants

Changed:

- P26 now has the complete five-artifact learning slice and lifecycle status
  `implemented`.
- Public catalogs describe the permanent P26 lesson, and a dedicated P26 suite
  covers its learner entry, numerical behavior, useful-signal preservation,
  failure/recovery, malformed inputs, resources, compatibility, isolation, and
  evidence boundary.
- This date-named retained evidence record was added.

Preserved:

- exact 84-module count, order, IDs, titles, folders, questions, phases, and
  implementation-batch mappings;
- contiguous implemented prefix through P26 and later-transition-safe
  historical tests;
- P25 and all unrelated module contents;
- ignored personal `.learning/` state and the global random stream;
- managed contracts, CI, verification infrastructure, architecture, tutor
  protocol, and every forbidden path; the already-dirty active-batch
  activation remains operator-owned and untouched;
- base-MATLAB implementation with no adaptive-filter black box, external I/O,
  persistent simulation state, worker, timer, or unbounded loop.

## Independent review and repair disposition

The independent system-risk review checked acceptance, path-change indexing,
LMS sign/update, stability language, useful-signal behavior, sweep isolation,
failure guards, recovery, resource ceilings, base-MATLAB compatibility,
frontier-safe tests, allowed paths, and claim boundaries. It found and repaired
two material gaps:

- the lesson conflated the mean-coefficient bound with mean-square stability,
  even though the broken white-input step lies below the former and above the
  latter; the lesson, walkthrough, checks, experiment reference bound, and
  regression coverage now distinguish them;
- the claimed preservation of the desired waveform had no behavioral gate;
  the experiment now asserts and retains pre/post coherent gain, and the new
  independent test checks gain plus correlation improvement in both epochs.

The earlier embedded carriage-return rendering defect remained repaired and
the dirty contract activation remained operator-owned. No other material DSP,
indexing, safety, compatibility, persistence, licensing, or scope defect was
found. MATLAB runtime and rendered-figure review remain unperformed.

## Rollback and recovery

Repository rollback removes the four added P26 implementation artifacts,
P26-named test/evidence, and P26 additions to allowed catalogs; restores the
P26 README scaffold wording; and restores only P26's manifest status to
`scaffolded`. P25 and P27–P84 canonical identities remain unchanged. The
operator-owned active-batch activation is outside this implementation's
rollback authority.

Runtime cancellation uses Ctrl+C. A full rerun deletes only P26-tagged figures,
reconstructs deterministic data from private seed 2601, resets LMS weights to
zero, and replaces P26 workspace values. It cannot restore unrelated caller
workspace variables overwritten before cancellation. There is no external
transaction or persisted simulation output to roll back.

## Residual risks and unperformed validation

- MATLAB and Octave syntax/runtime execution, actual script metrics, rendered
  figure inspection, and MATLAB version/toolbox reporting are unperformed
  because no compatible runtime is installed.
- The independent Python model supports the equations and qualitative/metric
  margins but cannot prove MATLAB syntax/runtime, MATLAB random samples,
  graphical quality, or numerical identity to a MATLAB run.
- Manual learner observation, teach-back, accessibility review, and
  representative instructional review are unperformed.
- Remote Control plane validation and target Quality CI are unperformed because
  no P26 PR/branch run exists. The target Quality command is run locally.
- Hardware/HIL, bench, field, RT1/RT2, real-time, operational radar, Unreal,
  signing, staging, production, release, and deployment validation are not
  applicable to this synthetic lesson and were not performed.

## Schema-aligned evidence summary

The governed batch permits this required Markdown evidence path but no
separate JSON evidence file. This embedded summary follows the repository
evidence schema without writing outside the batch scope.

```json
{
  "schema_version": 1,
  "batch_id": "P26",
  "status": "pass",
  "commands": [
    {"command": "python3 -m unittest discover -s tests -p test_p26_module.py -v", "exit_code": 0, "result": "18 tests passed"},
    {"command": "DSP_RADAR_VERIFY_PROFILE=contract python3 scripts/validate_curriculum.py", "exit_code": 0, "result": "84 modules, 26 implemented"},
    {"command": "DSP_RADAR_VERIFY_PROFILE=quick python3 -m unittest discover -s tests", "exit_code": 0, "result": "484 tests passed"},
    {"command": "DSP_RADAR_VERIFY_PROFILE=full ./scripts/agent-verify.sh", "exit_code": 0, "result": "curriculum and 484 tests passed"}
  ],
  "acceptance": [
    {"item": "guiding question preserved", "status": "pass", "class": "static"},
    {"item": "deterministic LMS, plots, two sweeps, path change, broken case", "status": "pass", "class": "static and independent model"},
    {"item": "concept-first lesson and limits", "status": "pass", "class": "static review"},
    {"item": "guided walkthrough and recovery", "status": "pass", "class": "static review"},
    {"item": "interpretation and prediction checks", "status": "pass", "class": "static review"},
    {"item": "dependencies and transparent operations", "status": "pass", "class": "static"},
    {"item": "implemented manifest integrity", "status": "pass", "class": "static"},
    {"item": "honest MATLAB runtime boundary", "status": "pass", "class": "evidence policy"}
  ],
  "changed_invariants": ["P26 scaffolded to complete implemented slice"],
  "residual_risks": ["MATLAB runtime and rendered figures unverified"],
  "unperformed_validation": ["MATLAB", "hardware", "field", "production"]
}
```
